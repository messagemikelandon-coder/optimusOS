# Current State

Purpose: concise operational snapshot of the verified current repository state.
Information owner: repository maintainers and the current Codex session author.
Read when: before every task, together with `SESSION_HANDOFF.md`.
Update when: the branch, working status, live stack status, migrations, or quality-gate results change.
Last verified date: 2026-07-22. The `/goal` multi-shop-pilot roadmap is in progress; use the **Latest merged status (2026-07-22)** note directly below, the section beneath it, and `docs/context/GOAL_EVIDENCE_MATRIX.md` for current status. Older module history below remains useful background but is not the authoritative branch marker.
Relevant sources: `git status --short --branch`, `git rev-parse HEAD`, `env UV_CACHE_DIR=/tmp/uv-cache uv run ruff format .`, `env UV_CACHE_DIR=/tmp/uv-cache uv run ruff check .`, `env UV_CACHE_DIR=/tmp/uv-cache uv run pyright`, `env UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`, `node --check app/static/app.js`, `docker compose config -q`, `docker compose build backend worker`, `docker compose up -d backend worker frontend`, `docker compose exec -T backend alembic heads`, `docker compose exec -T backend alembic upgrade head`, `docker compose exec -T backend alembic current`, `curl https://staging.optimus-os.com/health` and served-HTML marker checks, `gh pr list --state merged`, non-billable Playwright/API proof commands run from the local shell.

## Latest merged status (2026-07-23)

- **`main` HEAD:** `3def799` — the merge commit of **PR #87, "Diagnostic triage surfacing"**. **Migration head: `036_diagnostic_evidence`** (added by PR #86; PRs #85 and #87 add no migration).
- **Core-workflow `/goal` progress (2026-07-23):** four slices merged in order — Phase 2B runtime observability (PR #84), vehicle-first VIN-decode intake (PR #85, `af653d7`), Diagnostic Evidence Engine (PR #86, `33a4bf5`, migration `036`), and Diagnostic triage surfacing (PR #87, `3def799`). All CI-green; each independently correctness- and security-reviewed. See the sections below.
- **Deterministic Job Compiler (`/goal` Priority 1) — MERGED** via PR #89 (`93f96ff`, migration `037`; ADR-025). Standalone no-OpenAI service compiling an approved diagnostic finding into a priced draft job.
- **Customer-optional intake bridge (`/goal` Priority 2) — MERGED** via PR #90 (`9b4b2fc`, migration `038`; ADR-026). VIN-decoded draft entity + atomic, attach-aware conversion; `vehicles.customer_id` deliberately NOT made nullable.
- **Canonical release bridge — implemented on branch `agent/claude/release-bridge` (draft PR pending; NOT yet on `main`; ADR-027).** Releases an approved Job Compilation into a real DRAFT `Estimate` that flows through the existing approval/work-order/invoice pipeline — no parallel records, idempotent, transaction-safe. See the dedicated section below.
- **Phase 1 (security and structural foundation) is complete. Phase 2A (storage observability, ADR-023) and Phase 2B (bounded runtime observability, ADR-024) are both merged** (PRs #83, #84).
- Earlier milestone: **PR #81, "post-signup operating-mode onboarding" (ADR-022, owner-only, non-blocking)**, feature head `22cfada`, merged via `7050bb8`; context-sync PR #82 (`e924ffa`).
- **Operating-mode onboarding (ADR-022), what shipped and is now merged into `main`:**
  - Owner-only, **non-blocking** post-signup onboarding (`require_owner_only_context`; managers, technicians, support, and unauthenticated callers are denied). Account creation is never blocked on the choice.
  - Owners deliberately select **Solo / Mobile Field / Shop** after signup, via `GET /api/operating-mode/onboarding` and `POST /api/operating-mode/onboarding/complete` (reuses the mode-transition service's shared lock/optimistic-concurrency primitive and the capability matrix; writes exactly one `ShopEvent`, including for a no-op default `shop` confirmation; 409 on a stale `expected_current_mode`).
  - **Existing shops are grandfathered** (migration 035 backfilled `operating_mode_confirmed_at`, so they never see the card); **new shops remain unconfirmed** (`NULL`) until the owner selects.
  - **Capability-shaped navigation** is implemented as a frontend affordance over `GET /api/capabilities` — role visibility is evaluated first, and the capability pass can only hide further, never reveal what role hid.
  - **Bays remains OBSERVE-only** (capability-gate telemetry). **No capability enforcement has shipped** — an AST safeguard (`tests/test_capability_gate_safeguards.py`) fails the build if any route references `CapabilityGateMode.ENFORCE`. The first enforcement change, when it comes, is Bays alone, gated on its own observe-pilot evidence.
- Full ADR-022 slice history: `docs/architecture/OPERATING-MODES-ARCHITECTURE-BRIDGE.md` §12a–§12d and the ADR-022 implementation-status amendment. The `/goal` pilot-phase status (a separate 17-phase numbering) remains in `docs/context/GOAL_EVIDENCE_MATRIX.md`.

## Canonical release bridge — implemented on branch `agent/claude/release-bridge` (draft PR pending; NOT yet on `main`)

Additive slice (ADR-027) that releases an approved (owner-reviewed) Job Compilation into a real canonical `Estimate`, reusing the estimate/approval/work-order/invoice/event pipeline. Do not treat as merged until it lands on `main`.

- New owner/manager-gated route `POST /api/job-compilations/{id}/release` → `app/job_release.py::release_job_compilation`. Builds a valid `EstimateResponse`/`EstimateRequest` from the compilation's stored snapshot and persists a DRAFT `Estimate` via `app/estimate_store.py::create_estimate_from_payload` (deterministic, no orchestrator; reuses the exact `Estimate`/`EstimateRevision` validation/snapshot/numbering/content-hash as the AI `create_estimate`). **No parallel records.**
- **Idempotent** on a new `job_compilations.released_estimate_id` FK (+ `released_at`/`released_by_user_id`, migration `039`; head now `039`); **row-locked** and committed in **one transaction** (`create_estimate_from_payload(commit=False)` → link → single commit), so no duplicate estimate/lines/tasks/events. Rejects a **superseded** (stale) compilation, **cross-shop** (404), and empty/invalid-price (via `_validate_generated_estimate`). Records a `released` event.
- Preserves labor, **customer-priced parts only** (no supplier cost), totals, and the source finding's confidence/severity/conclusion (into the estimate research bundle: confidence→research confidence, severity→warnings/risk flags). Never auto-sends, auto-approves, orders parts, or captures payment — the estimate is created **DRAFT**; sending for customer approval (and thus work-order activation) stays the existing pipeline.
- Two minimal, backward-compatible shared-model changes unblock the reuse (ADR-025 impedance): `SelectedPart.url`, `EstimateRequest.location`, and `EstimateRequest.vehicle` become **optional** — an in-house catalog part has no retailer URL and a deterministic release has no geographic location, so both store honest absence, not a fabricated value. The AI create-input (`EstimateRecordBase.location`) stays required; the AI path still sets `SelectedPart.url`; the one frontend "Open source" part link is now conditional ("In-house catalog" when absent).
- Frontend: a "Release to estimate" button on the compiled-job panel (owner-gated); shows "Released → estimate #N" once linked. CSP-safe.
- Gates: `git diff --check`, ruff format/check, pyright clean; `node --check` clean; fast suite **842 passed, 2 skipped** (+7 release API tests, +1 UI wiring test); role-isolation + capability-safeguards green (new route owner-gated; no `ENFORCE`); **real-Postgres migration round-trip (`tests/e2e/test_job_compilation_release_migration.py`) verified locally** (038→039 adds columns + widens the event CHECK to allow `released`, downgrade reverses). Independent correctness + security reviews performed on-branch. **Not yet merged.**

## Customer-optional intake bridge (`/goal` Priority 2) — MERGED into `main` via PR #90 (`9b4b2fc`)

Additive slice (ADR-026) that lets a shop hold VIN-decoded vehicle data before a customer exists, then convert atomically. **`vehicles.customer_id` is NOT made nullable.** Do not treat as merged until it lands on `main`.

- Migration `038_intake_vehicle_draft` adds seven **nullable** structured vehicle columns to `intake_requests` (`vehicle_vin`, `vehicle_year/make/model/trim/engine/drivetrain`) so a draft can carry an identified vehicle before any customer or canonical `vehicles` row exists. Head is now `038`.
- `convert_intake_request` (`app/intake_store.py`) now: defaults the vehicle from the draft's stored fields (conversion-payload fields override); **optionally attaches to an explicit existing same-shop customer** (`IntakeRequestConvertRequest.customer_id`, validated present + same-shop via shop-scoped `get_customer` + not archived — cross-shop/missing rejected as not-found, archived rejected as 409) instead of always creating a new customer; creates customer (when new) + vehicle in **one transaction** via a new backward-compatible `commit: bool = True` param on `create_customer`/`create_vehicle`, so a duplicate-VIN (or any) failure rolls back and **never leaves an orphan customer** (also fixes a latent orphan bug in the prior flow); surfaces duplicate active VIN and double-conversion as clean `409`s. Row-lock + double-conversion guard preserved.
- Frontend: the Service Desk intake form gains a VIN + Decode-VIN affordance (reusing `POST /api/vehicles/decode-vin`) and structured year/make/model/trim/engine/drivetrain fields; the convert form gains an optional existing-customer-ID attach input. CSP-safe; decode populates only empty fields.
- Estimate/work-order/invoice invariants preserved: conversion still yields canonical `Customer`/`Vehicle` rows; nothing is customer-optional at the canonical layer.
- Additive only: migration `038` adds nullable columns (downgrade drops them); no existing route/behavior changes (the `commit` param defaults `True` for every existing caller); OpenAPI gains fields on the intake schemas. Rollback = revert the commit(s) + `alembic downgrade 037_job_compilations`.
- Gates: `git diff --check`, ruff format/check, pyright clean; `node --check app/static/app.js` clean; fast suite **832 passed, 2 skipped** (+11 intake-bridge API tests, +1 UI wiring test); role-isolation + capability-safeguards green (routes unchanged/owner-gated; no `CapabilityGateMode.ENFORCE`); **real-Postgres migration round-trip (`tests/e2e/test_intake_vehicle_migration.py`) verified locally on Docker Postgres 16** (037→038 adds the seven columns, downgrade removes them, re-upgrade restores). Independent correctness + security reviews performed on-branch. **Not yet merged.**
- **Deferred (documented):** a full customer-typeahead picker in the convert UI (v1 uses a numeric existing-customer-ID input); auto-decoding a VIN on paste at intake.

## Deterministic Job Compiler (`/goal` Priority 1) — MERGED into `main` via PR #89 (`93f96ff`)

Now merged (all 6 CI checks green). The section below is retained as the slice record.

Standalone deterministic domain (ADR-025) that compiles an **approved diagnostic finding** into a priced draft job with **no OpenAI / no paid call**. Merged into `main` via PR #89 (merge commit `93f96ff`); migration head `037`.

- New service `app/job_compiler.py` + tables `job_compilations` / `job_compilation_events` (migration `037_job_compilations`, head is now `037`). Four owner/manager-gated routes: `POST /api/diagnostic-findings/{id}/compile-job`, `GET /api/job-compilations`, `GET /api/job-compilations/{id}`, `GET /api/job-compilations/{id}/events`.
- `compile_job` deterministically expands an owner-validated request (recommended services with labor hours, an explicit labor rate, optional shop-supplies/parts-tax percentages, and part references) into labor lines, **aggregated part needs priced from the catalog customer `unit_price` only** (supplier `unit_cost`/markup never read or exposed — verified in the OpenAPI schema, persisted JSON, and UI), work-order task descriptors, and reconciled `Decimal` totals. Pure Python; no network/orchestrator.
- **Never** sends, approves, orders parts, alters customer records, or takes payment. A compilation is an internal draft (`released` defaults False); customer release stays the separate existing estimate-approval flow. **Idempotent**: `content_hash` over normalized inputs + the finding's evidence snapshot — identical inputs against an unchanged diagnosis are a no-op; changed inputs (or a changed diagnosis) supersede the prior draft (`superseded_by_id`, `status='superseded'`) and create the next `revision_number`. Source finding row-locked during compile. Reject rules: finding must be same-shop, not archived, and have a recorded `conclusion`; each part must be same-shop with a customer price. Severity/confidence/conclusion + the derived `diagnosis_unverified` flag are snapshotted; append-only event trail (compiled/recompiled/superseded).
- Frontend: an owner/manager-only "Compile job from finding" panel in the diagnostics view (labor services + fees; renders labor/parts/tasks/totals; loads any existing draft on finding select). CSP-safe (no inline script/style); all interpolation escaped. **v1 UI covers labor services + fees; a per-service parts picker is a documented follow-up — the API fully supports parts (tested).**
- Additive only: migration `037` adds two tables (downgrade drops them); no existing route/behavior changes; OpenAPI gains 4 owner-gated paths. Rollback = revert the two commits + `alembic downgrade 036_diagnostic_evidence`.
- Gates: `git diff --check`, ruff format/check, pyright clean; `node --check app/static/app.js` clean; fast suite **819 passed, 2 skipped** (+12 job-compiler API tests, +1 UI wiring test); role-isolation + capability-safeguards green (new routes auto-covered; no `CapabilityGateMode.ENFORCE`); **real-Postgres migration round-trip (`tests/e2e/test_job_compilation_migration.py`) verified locally on Docker Postgres 16** (036→037 creates both tables + status CHECK, downgrade removes them, re-upgrade restores). Independent correctness (one MAJOR idempotency-hash fix applied) + security (PASS) reviews done. **Merged into `main` via PR #89 (`93f96ff`); all 6 CI checks green.**
- **Deferred (documented):** releasing a compiled draft into the canonical Estimate/WorkOrder/Invoice via the existing owner-approved approval flow (this compiler produces its deterministic input); AI-proposed compile inputs (deterministic validation stays the gate); per-service parts picker + optional severity-priority ordering in the UI.

## Diagnostic triage surfacing — MERGED into `main` via PR #87 (`3def799`)

Additive triage layer over the Diagnostic Evidence Engine (no migration). `list_diagnostic_findings` gained an optional `severity` filter and `GET /api/diagnostic-findings` a bounded `?severity=` query param (`DiagnosticSeverity` enum, default behavior unchanged); the diagnostics list toolbar gained a Safety-severity filter and each list row now renders a compact severity chip plus an "Unverified" chip (when a conclusion has no confidence) so an unsafe or un-evidenced finding stands out at triage. Owner/technician gating and tenant scoping unchanged. Correctness+security review CLEAN; gates at merge: fast suite 807 passed/2 skipped (+1 severity-filter test), CI 6/6 green.

## Diagnostic Evidence Engine (`/goal` slice 3) — MERGED into `main` via PR #86 (`33a4bf5`)

`/goal` core-workflow slice 3. Additive enrichment of the existing `diagnostic_findings` domain (migration `036_diagnostic_evidence`).

- Adds four nullable columns to `diagnostic_findings` — `complaint` (operator-reported complaint, distinct from the technician-observed `symptoms`), `confidence` (`theory` / `probable` / `confirmed`), `severity` (`informational` / `advisory` / `service_soon` / `unsafe`), and `recommended_next_test` — plus two CHECK constraints bounding the enum columns to their allowed values (or NULL). New Pydantic enums `DiagnosticConfidence`/`DiagnosticSeverity`; the fields flow through `DiagnosticFindingBase`/`Create`/`Update`/`Read` and the existing owner/technician-gated CRUD + append-only event trail unchanged.
- **"No unsupported diagnosis stated as fact" is enforced at the read/presentation layer, not by a write-time block:** `DiagnosticFindingRead` carries a derived `diagnosis_unverified` flag (True when a `conclusion` is on record but no `confidence` level was stated). The frontend renders such a conclusion tagged **"Unverified working theory,"** and shows severity/confidence badges. This deliberately preserves backward compatibility (a conclusion may still be saved without a confidence — no new 422, no external API break; the four existing conclusion-without-confidence tests remain green) while guaranteeing an un-evidenced diagnosis is never *presented* as fact.
- Additive only: migration `036` adds columns + constraints (downgrade drops them); no existing route/behavior changes; OpenAPI gains the new fields on the diagnostic-finding schemas. Rollback = revert the single commit + `alembic downgrade 035_operating_mode_confirmed_at`.
- Gates: `git diff --check`, ruff format/check, pyright, `node --check app/static/app.js` all clean; fast suite **806 passed, 2 skipped** (+4 diagnostics API tests: evidence round-trip, unverified-flag on/off, invalid-enum rejection); role-isolation, capability-safeguards, and migration-compat pass; **E2E migration round-trip (`tests/e2e/test_diagnostic_evidence_migration.py`) verified on real Postgres** (035→head column/constraint presence, downgrade removal, re-upgrade). Correctness + security reviews CLEAN; CI 6/6 green. **Merged into `main` via PR #86 (`33a4bf5`).**

## VIN-decode vehicle intake (`/goal` slice 2) — MERGED into `main` via PR #85 (`af653d7`)

Vehicle-first foundation slice (`/goal` order item 2). Additive; merged into `main`.

- New endpoint `POST /api/vehicles/decode-vin`, owner/manager-gated (`OwnerAuthContextDep`; classified by the existing `tests/test_role_isolation.py` static audit with no allowlist change). Given a 17-character VIN it resolves year/make/model/trim/engine/drivetrain for **automatic vehicle-field population** with **no customer record required**. Read-only lookup (creates/mutates nothing), rate-limited per client (`MAX_VIN_DECODE_REQUESTS_PER_MINUTE`, default 20) because it triggers one outbound NHTSA vPIC lookup, and sets `Cache-Control: no-store`.
- **Safe-failure by design:** `app/services/vin.py::VinService.decode_intake()` wraps the existing NHTSA decode and, on any transport/parse error or empty upstream result, returns a `VinDecodeStatus.UNAVAILABLE` response (HTTP 200) with a manual-entry message rather than a 5xx. A VIN that decodes but resolves no identity fields is also reported `unavailable`, so an un-decoded VIN is never presented as a confirmed vehicle. `decoded`/`partial` classify full vs. incomplete year/make/model.
- Frontend: a "Decode VIN" button + live status on the vehicle intake form (`app/static/index.html`/`app.js`/`styles.css`); the handler populates **only empty** identity fields, never clobbering hand-entered values, and uses `textContent` (no XSS surface).
- The VIN service is a FastAPI dependency (`get_vin_service`) restricted to the `vpic.nhtsa.dot.gov` host allowlist the research orchestrator already uses, and is overridable in tests so no test makes a real outbound call.
- Additive only: no migration (head stays `035_operating_mode_confirmed_at`), no schema, no change to any existing route/behavior; OpenAPI gains one POST. Rollback = revert the single commit.
- Independent correctness + security reviews (optimus-reviewer, optimus-security-reviewer) both returned CLEAN-to-merge with two non-blocking items, both fixed same-session: (1) `VinService.decode` now guards against a non-dict upstream JSON body / non-dict first result (raising `ValueError`, which `decode_intake` already degrades to `UNAVAILABLE`) so a malformed-but-valid-JSON upstream response can never escape safe-failure as a 5xx; (2) the frontend decode POST now sends `Content-Type: application/json`, matching every other JSON POST in `app.js`.
- Gates at merge: ruff format/check + pyright clean; `node --check app/static/app.js` clean; fast suite **802 passed, 2 skipped** (+16 new tests: `tests/test_vin.py` safe-failure/classification/malformed-shape units, `tests/test_vin_decode_api.py` auth/decoded/soft-failure/invalid-VIN/no-store/rate-limit). Full CI green after one unrelated E2E flake (password-reset UI timeout) cleared on re-run. **Merged into `main` via PR #85 (`af653d7`).**

## Phase 2B — bounded runtime observability summary (branch, draft PR pending — NOT yet on `main`)

Second Phase 2 (observability) slice, additive on top of merged Phase 2A. Implemented on branch `agent/claude/phase2b-runtime-observability` off `main` at `b81aad5`; awaiting draft-PR review and merge. Do not treat as merged until it lands on `main` (ADR-024).

- New read-only endpoint `GET /api/operations/summary`, gated **support-only** (same `require_support_context` gate and rejections as Phase 2A). Consolidates six non-sensitive process signals: (1) request traffic/latency from an in-process metrics registry (`app/runtime_metrics.py`) the request-context middleware feeds, labelled by **route template** (never a raw path/id), with capped label cardinality; (2) Postgres/Redis TCP reachability (same fail-safe probe as `/ready`); (3) background-worker heartbeat — the worker (`scripts/optimus_worker.py`) writes a fixed Redis key with a bounded TTL (validated ≥ 2× its write interval; value = one epoch second, no job/customer data) and the summary reads `alive`/`stale`/`missing`/`unknown`; (4) work-queue condition, **off by default** (`WORKER_QUEUE_REDIS_KEY` empty ⇒ `not_configured`, no Redis touched; there is no application queue today and ADR-014 records a future queue would be Postgres `SKIP LOCKED`, not Redis); (5) OBSERVE-only capability-decision counters (`app/capability_metrics.py`); (6) the Phase 2A storage snapshot **reused from cache** (new `peek_snapshot()`, never re-collected — the summary never launches `docker system df`).
- Bounded like Phase 2A: dependency/worker/queue signals served from a TTL cache + single-flight (`app/runtime_monitor.py`); endpoint rate-limited (existing registry), sets `Cache-Control: no-store`; a throttled secret-free `reliability_event: runtime.degraded` identifies the support actor by role + internal id only. New settings: `WORKER_HEARTBEAT_REDIS_KEY`/`WORKER_HEARTBEAT_INTERVAL_SECONDS`(30)/`WORKER_HEARTBEAT_TTL_SECONDS`(150, ≥ 2× interval validated), `WORKER_QUEUE_REDIS_KEY`(""), `RUNTIME_SNAPSHOT_TTL_SECONDS`(15), `DEPENDENCY_PROBE_TIMEOUT_SECONDS`(1.0), `RUNTIME_WARNING_COOLDOWN_SECONDS`(300), `MAX_OPERATIONS_SUMMARY_REQUESTS_PER_MINUTE`(30).
- **No enforcement, no host access, no mutation.** Bays stays OBSERVE-only (AST safeguard untouched); no Docker socket / DB volume mounted; process-visibility only, not production host monitoring (same boundary as Phase 2A). The still-present worker gains one small bounded responsibility (heartbeat), partly superseding `PHASE2-READINESS.md` §2A's never-executed "remove the worker" evaluation.
- Additive only: no migration, no schema, no change to any existing route/behavior; OpenAPI gains one GET. Rollback = revert the single commit. Documented in `MONITORING.md` §6 and ADR-024.
- Gates: ruff/pyright clean; fast suite **786 passed, 2 skipped** (+79 new tests across runtime-metrics, capability-metrics, runtime-monitor, middleware, config-validation, summary API/authorization, peek, and route-audit). Full Docker/Playwright e2e not run in this container (no Docker/Postgres/Redis) — CI's job.

## Phase 2A — read-only disk/Docker-storage observability (merged into `main` via PR #83, `b81aad5`; ADR-023)

First Phase 2 (observability) slice. Merged into `main` (PR #83, merge commit `b81aad5`).

- New read-only endpoint `GET /api/operations/storage`, gated **support-only** (reuses `require_support_context`/`SupportAuthContextDep`). It is platform-infrastructure telemetry, so owners, managers, technicians, suspended-shop accounts, unauthenticated callers, and impersonated-owner sessions (role `owner`) are all denied. Returns host filesystem total/used/available/percent + `ok`/`warning`/`critical`/`unknown` status, plus aggregate Docker storage (images/containers/volumes/build_cache) via read-only `docker system df`, distinguishing `unavailable`/partial/malformed-measurement Docker output (fails closed) from a healthy zero-usage host. **This is process-visibility only, not production host monitoring.**
- New stdlib-only leaf collector `app/storage_monitor.py` (dependency-injected `disk_usage=`/`run=`; never raises, never mutates) + a bounded-collection service `app/operations_monitor.py` (TTL cache + single-flight so at most one `docker system df` runs per TTL window; throttles the warning to a severity transition or cooldown). Endpoint is rate-limited (existing limiter registry), sets `Cache-Control: no-store`, and exposes a non-sensitive **label** (`STORAGE_TARGET_LABEL`) — the raw `DISK_MONITOR_PATH` is never returned or logged. Settings: `DISK_WARNING_PERCENT`/`DISK_CRITICAL_PERCENT` (80/90, warning≤critical validated), `STORAGE_SNAPSHOT_TTL_SECONDS` (30), `STORAGE_WARNING_COOLDOWN_SECONDS` (300), `MAX_OPERATIONS_STORAGE_REQUESTS_PER_MINUTE` (30). Threshold crossings emit one throttled secret-free `reliability_event` identifying the support actor by role + internal id.
- **Deployment boundary:** no Docker socket or database volume is mounted into the web backend, and none is authorized here. Real host/Docker monitoring is a separate least-privileged host collector/sidecar/hosting-metric/script — see `docs/context/MONITORING.md` §4.
- Additive only: no migration, no schema, no change to any existing route/behavior; OpenAPI gains one GET. Rollback = revert the single commit.
- Gates at merge: ruff/pyright/`node --check` clean; fast suite **707 passed, 2 skipped** (+81 new tests). Full Docker/Playwright e2e is CI's job.

## `/goal` Multi-Shop Pilot Program

- Full requirement-by-requirement status lives in `docs/context/GOAL_EVIDENCE_MATRIX.md` — not duplicated here to avoid drift between two copies of the same table.
- Synced `main` HEAD: `7050bb8` (PR #81, operating-mode onboarding) — see the **Latest merged status** note above.
- Phase 8 platform support administration is **merged into `main`**: a read-only cross-shop directory for a shop-less `support` role plus time-boxed owner impersonation (migration `033_support_impersonation`, chained off `032_support_role`). It sits well behind the current head (`035`). The evidence matrix records it under "Support administration domain". There is **no in-flight support-admin worktree or unmerged support-admin branch** — any earlier note to that effect is superseded.
- The remaining pilot phases are tracked in the evidence matrix. The historical per-module narratives below predate the operating-mode slices and are retained only as background.

### Historical detail — Phase 8 support administration (merged; retained for context only)

The following records how the Phase 8 slice was built and reviewed at the time. It is historical and must not be read as current in-flight work — the slice is merged into `main`.

- Two parts: (1) a read-only cross-shop directory for a shop-less `support` role (`GET /api/support/shops`); (2) **impersonation** — a support account can mint a real, time-boxed (60 min) session as any shop's owner (`POST /api/support/shops/{id}/impersonate`), reusing existing owner-gated routes with zero new access-control branching, and end it (`POST /api/support/end-impersonation`); each start/end is logged as a `ShopEvent` on the target shop (`support_impersonation_started`/`_ended`).
- Independent security review at the time found one Medium and three Low/informational items, all fixed same-session: a lazy audit-reconciliation sweep for abandoned/expired impersonation sessions (`app/auth.py::reconcile_abandoned_impersonation_sessions`); a stricter fail-closed owner-resolution check in `app/support_store.py::_owner_for`; a row-lock on `end_impersonation_session` closing a double-submit race; and a frontend confirm-before-impersonating step. A correctness-review finding (a read path calling the writing `sync_shop_access_status`) was fixed with a pure `is_shop_access_suspended_readonly` helper.
- Point-in-time gate numbers from that slice (superseded by the current baseline): fast suite 487 passed / 2 skipped, e2e 38 passed.

## Operational Snapshot

- Active development phase: **everything through Phase 5.6 (all 8 sub-phases, 0 through 7) is merged to `main` and closed.** Merged to `main`, in order: Landing Page Redesign (PR #14), Overview Dashboard + Approval Queue (PR #15, reverted by PR #16, restored via `agent/claude/restore-dashboard`), Phase 5.6 sub-phases 0-2 — nav cleanup + auth foundation + Technicians (PR #17), a second landing-page/shop-management UI redesign (PR #19), Phase 5.6 sub-phases 3/4/6/7 — Vendors+Parts, Service Desk, Diagnostics+Inspections, Reports (PR #20), an estimator-draft full-width layout fix + collapsible evidence-standard rail (PR #21), removal of the estimator's decorative "readiness" gauge box (PR #22), and Phase 5.6 sub-phase 5 — Scheduling/appointments (PR #23). `main` HEAD is `077f4d3`. Every "Coming soon" nav stub from the original Overview Dashboard slice is now a real, merged module. **Not deployed to staging in full** — see the corrected Staging Prep section below; staging is currently running a commit after PR #21 but before PR #22/#23 (verified live via served-HTML markers, not assumed).
  All gates re-verified from a clean checkout of `origin/main` (not the working tree used to build any of this): `ruff format --check .` clean (107 files), `ruff check .` clean, `pyright` 0 errors, `pytest -q` **278 passed**, `node --check app/static/app.js` clean, `docker compose config -q` and the `ops/docker-compose.staging.yml` overlay both valid, Alembic chain linear and single-headed at `016_scheduling`.
- Current branch used for active work: `agent/claude/shop-management-ui`. As of this verification pass its local worktree is clean (only the pre-existing untracked `optimusOS/` stray directory remains) — all prior in-flight work on it has been committed and merged (PRs #20-#23). **Correction to prior versions of this doc**: two different Claude sessions had uncommitted, unpushed work sitting on this same branch/worktree simultaneously (a `AGENTS.md` violation — "Claude and Codex must never edit the same worktree concurrently"). Both sessions' work was independently committed (in one case by hand-isolating one session's small hunk out of the other session's much larger concurrent diff via a manual patch, to avoid sweeping in unrelated in-flight work) and both merged without any lost work or conflicts. See `docs/context/KNOWN_ISSUES.md`'s Historical Resolved Issues for the full account. Do not assume single-session exclusivity on this branch going forward without checking `git status` first.
- Git working state: Phase 2 merged via PR #6 (`8be1dba`); Phase 3 via PR #7 (`423192b`); Phase 4 via PR #8 (`c920891`); Phase 5 staging prep via PR #9/#10; Phase 5.5 via PR #12/#13; Phase 5.6 (all sub-phases) via PR #17/#19/#20/#23; the estimator layout fixes via PR #21/#22 — all merged into `main`. Old remote feature branches were deleted on GitHub after each merge except `agent/claude/shop-management-ui`, which is still in active use.
- Auth baseline status: the owner-session, customer, vehicle, context, and estimate-approval slices remain in place and unchanged in scope.
- Current verified functionality: owner login/logout/me, protected chat, protected location resolution, owner-scoped context CRUD, customer CRUD/list/search/archive, vehicle CRUD/list/search/archive, saved estimate CRUD/revisioning/approval, Work Order conversion/list/detail/update/status/note flows, invoice generation/list/detail/issue/html/pdf flows, invoice payment recording/void/schedule/balance-derivation flows, notifications, and Square sandbox invoice push/refresh.

## Landing Page Redesign (merged to main via PR #14)

- Goal (owner request, 2026-07-10): add a new unauthenticated marketing landing page at `/` with working login access, shown before the authenticated dashboard ("OS system"), and apply the same graphite/off-white/steel/restrained-red palette and existing 3D rotor/caliper/diagnostic-tablet visual motif consistently across the whole authenticated app (replacing the previous electric-blue palette). This was an explicit, intentional request to re-theme the whole app, not scope creep.
- Files changed: `app/static/index.html` (new `<section class="marketing-site" data-view-panel="landing">` before `.app-shell`, reusing the existing `.hero-panel`/`.mechanic-stage`/`.capability-card`/`.principle-tags` classes for visual consistency, `<body class="marketing-mode">` default), `app/static/styles.css` (root CSS custom properties re-themed from blue to graphite/off-white/steel/red; ~35 hardcoded color literals throughout the file updated to match; new marketing-page-only CSS and a `.marketing-mode` body-class toggle appended; a global `:focus-visible` outline rule added), `app/static/app.js` (unauthenticated visitors to `/` now stay on the marketing landing page instead of being forced to `/login`; `/login`/`/approval` explicitly clear `marketing-mode` from the existing pathname branch in `initializeApp`), `tests/test_official_ui.py` (three new regression tests, see below).
- No real Landon Motor Works field photography was available, so the hero visual reuses the existing CSS-drawn 3D rotor/caliper/diagnostic-tablet scene (already used on the dashboard), recolored to match — not fabricated photos.
- Two CSP violations were found and fixed during this work (full detail in `KNOWN_ISSUES.md`): an inline `<script>` bootstrap (would have broken `/login`/`/approval` under the real `script-src 'self'` CSP — caught by independent review before it shipped) and a **pre-existing, unrelated** inline `style=""` attribute on the Square dashboard panel (violates `style-src 'self'`, present since the Square feature slice, never previously checked against a live CSP). Both fixed; regression tests added (`test_index_html_has_no_inline_scripts`, `test_index_html_has_no_inline_style_attributes`).
- Verification: `ruff format`/`ruff check .` clean, `pyright` 0 errors, `node --check app/static/app.js` clean, `pytest -q` 203 passed (200 pre-existing + 3 new: the two CSP regression tests above plus `test_marketing_landing_page_gating`). Independent review completed (`optimus-reviewer` agent) — one CRITICAL finding (the inline-script CSP issue) and fixed before this doc was written; re-verified live afterward.
- Live proof (2026-07-10): rebuilt the `backend` Docker image (which bakes in `app/static` at build time, unlike the `frontend` nginx service which bind-mounts it) and ran a real Playwright browser directly against `http://127.0.0.1:8000` (the only local surface that actually applies the FastAPI `security_headers` CSP middleware — the dev nginx frontend at `:5173` serves `app/static` via a bind-mounted `try_files` and does not route `/`/`/login`/`/approval` through the backend, so it does not exercise the CSP at all). Confirmed: unauthenticated `/` shows the marketing section with `.app-shell` hidden; `/login` and `/approval` correctly show the app shell with the right view active; zero CSP console violations on any of the three routes after the fixes (only the expected `401` on `GET /api/auth/me`, matching the project's established "expected auth guard" pattern).
- Not done / explicitly out of scope this session: no authenticated-dashboard screenshot was captured (no synthetic or real owner session was minted, consistent with this project's established caution around session-credential materialization) — the dashboard's re-theme was instead verified by confirming it shares the exact same CSS classes/variables already visually confirmed correct on the login page and landing page (both screenshotted).
- **Merged to `main` via PR #14** (2026-07-10). Not deployed to staging.

## Overview Dashboard & Approval Queue (merged to main, currently LIVE — full history below)

**This module has flip-flopped and the current state (as of 2026-07-12) is: present and live.** Timeline, for context on the history in this doc and in `KNOWN_ISSUES.md`:
1. Originally shipped in commit `97e8b9d`, merged to `main` via PR #15 (2026-07-11).
2. Phase 5.6 sub-phases 0-2 (auth foundation + Technicians module) then merged via PR #17, touching the same files (`app/main.py`, `app/static/index.html`/`app.js`) without removing the dashboard.
3. Owner requested a full revert of the dashboard; GitHub's auto-generated revert PR (#16) had merge conflicts against the by-then-changed `main`, hand-resolved to remove the dashboard/Approval Queue while preserving the Technicians module, merged 2026-07-12.
4. Owner then asked for the dashboard back. Resolved with `git revert -m 1` of PR #16's merge commit (a clean "revert the revert," no conflicts since nothing touched those files in between) on branch `agent/claude/restore-dashboard`, restoring the dashboard/Approval Queue exactly as it was, with the Technicians module and auth foundation still intact and unaffected throughout.

The original implementation detail (unchanged by the revert/restore round-trip) is preserved below for reference:

- Goal (owner request, 2026-07-11, following a `/plan` cycle the owner approved before implementation): replace the "Shop intelligence online" dashboard hero with a real, data-driven shop-management overview dashboard, using an owner-supplied reference financial-dashboard image as a **structural** reference only (layout/density/hierarchy) — not its branding, labels, colors, or values. Two scope decisions were confirmed with the owner up front: (1) the left nav gets real modules only, plus clearly-disabled "Coming soon" stubs for modules with zero backing data today (Service Desk, Diagnostics, Inspections, Scheduling, Technicians, Parts, Vendors, Reports); (2) charts use a vendored Chart.js rather than hand-rolled SVG.
- New backend: `app/dashboard_store.py` (new, read-only, owner-scoped aggregation module, same pattern as `customer_history_store.py`) + `GET /api/dashboard/summary?date_from=&date_to=` in `app/main.py` + new Pydantic response models in `app/models.py` (`DashboardSummaryResponse` and friends). No schema/migration changes — pure aggregation over existing `Invoice`/`WorkOrder`/`Estimate`/`InvoicePayment`/`PaymentSchedule`/`EstimateApprovalRequest` tables.
- Honesty guarantee: metrics with no real backing (**Net Profit**, **Technician Utilization**, **Bay Utilization**) are never fabricated — OptimusOS has no operating-expense tracking and no technician/bay-scheduling data model, so these render an honest "not available" state with the specific reason. **Gross Profit** and **Gross Profit Margin** were added for real as of Phase 6 Part G Slice 2a (2026-07-16), once Part F's Part Allocation work gave the codebase a real per-unit parts-cost record (`PartAllocation.unit_cost_snapshot`) — see `docs/context/PLANS.md`'s Part G Slice 2a entry and the "usage-period" COGS approximation documented in `docs/context/KNOWN_ISSUES.md` (cost isn't matched to the exact revenue it corresponds to, since no FK links invoice line items back to the `Part` catalog). Real metrics (Revenue, Labor/Parts Revenue, Average Repair Order, Gross Profit, Gross Profit Margin, Open Work Orders, Awaiting Customer Approval, Approval Conversion Rate, Accounts Receivable Health, revenue-by-category breakdown, current-operations counts, financial obligations incl. real upcoming `PaymentSchedule` installments) are computed from real data with period-over-period comparison deltas.
- Shop Insights panel: server-side, rule-based (not AI-generated, labeled honestly) — overdue invoices, completed-but-not-invoiced work orders, work orders stalled in `waiting_for_parts` (using the actual status-transition timestamp, not `updated_at`, to avoid understating wait time), estimates stalled in `awaiting_approval` (using the actual `EstimateApprovalRequest.created_at` send time), and a declining-average-repair-order trend check. Each insight links to the real record.
- New **Approval Queue** nav item + `#view-approval-queue`: fully real, built entirely on the already-existing `GET /api/estimates?status=awaiting_approval` endpoint (no new backend route needed) — list + detail + "Open in estimator"/"Open customer" links, a nav badge, and 60s background polling matching the existing notifications-badge pattern.
- Chart.js v4.4.4 UMD build vendored (owner-approved exact source URL) into `app/static/vendor/chart.umd.min.js` + `LICENSE-chart.js` (MIT), loaded via a same-origin `<script src="/static/vendor/chart.umd.min.js" defer>` tag — `script-src 'self'`-compliant.
- The dashboard's pre-existing real, working features (the "Direct command" quick-chat box and the "Live systems" health panel) were preserved, just relocated into the new layout — not deleted.
- A real bug was found and fixed during live verification: Chart.js's `responsive:true` mode needs a CSS-sized parent container, not just canvas width/height attributes — without it, the per-metric-card sparkline canvases rendered with unbounded height and blew out the whole metric-card grid row. Fixed by wrapping each sparkline canvas in a `.metric-sparkline-frame` div with an explicit CSS height.
- Verification: `ruff format`/`ruff check .` clean, `pyright` 0 errors, `node --check app/static/app.js` clean, `pytest -q` 210 passed (203 prior + 6 new backend tests in `tests/test_dashboard_api.py` covering empty state, real-data aggregation correctness, waiting-on-parts counting, invalid date range rejection, and cross-user isolation, + 1 new frontend markup regression test in `tests/test_official_ui.py`).
- Live proof (2026-07-11): rebuilt the `backend` Docker image and, inside that container, seeded a **synthetic** owner (`dashboard-verify-*`, not the real business owner) with a real estimate → work order → completed → issued-invoice chain, a second work order left in `waiting_for_parts`, and a third estimate sent for real customer approval — all via the app's own store functions, no fabricated production data. Logged in as that synthetic user with a real Playwright browser against `http://127.0.0.1:8000` (the CSP-enforcing surface): confirmed real numbers flow through every metric card/gauge/chart/panel, the date-range filter re-fetches without a page reload, the Approval Queue lists and opens the real pending estimate, disabled nav stubs are correctly inert (Playwright's own `aria-disabled`-aware actionability check refused a normal click; a forced click also did nothing), mobile layout is usable, and there were **zero CSP violations and zero console errors**. The synthetic user and all its data were deleted afterward (cascade delete via the owner row) — nothing synthetic remains in the dev database.
- Not done / explicitly out of scope: no permanent automated Playwright test was added for this live click-through (matches this repo's existing pattern of one-time live proofs rather than committed browser-test infrastructure).
- **Merged to `main`** — see the flip-flop timeline at the top of this section. Not deployed to staging.

## Phase 5.6 Sub-phase 0 & 1 — Nav Cleanup + Multi-Role Authorization Foundation (merged to main via PR #17)

- Sub-phase 0 (trivial): removed the "Talk to Optimus" nav entry from the desktop sidebar and the mobile bottom nav in `app/static/index.html`. Chat remains reachable via the Overview page's "Direct command" panel and quick-prompt buttons, which already call `navigate("chat")` (`app/static/app.js`) — no functionality lost.
- Sub-phase 1 (the multi-role authorization foundation — full spec in `docs/context/PLANS.md`'s Phase 5.6 section): OptimusOS's first owner/technician role split. This is plumbing only — no technician can be created yet (no provisioning endpoint until sub-phase 2), so today it's inert for the real owner account and only changes behavior once a technician row exists.
- New migration `alembic/versions/011_multi_role_auth.py`: `UserAccount.shop_owner_id` (nullable self-referencing FK, `ondelete="CASCADE"`, indexed) and a `CHECK (role IN ('owner','technician'))` constraint, mirrored in `app/db_models.py`'s `UserAccount.__table_args__` so SQLite test runs (which build schema from the ORM models, not migrations) enforce it too.
- New `app/auth.py` helpers: `effective_owner_id(auth)` (returns `auth.user.id` for an owner, `auth.user.shop_owner_id` for a technician; raises `403` if a technician has no `shop_owner_id`), `require_role(auth, *allowed)`, and `require_owner_context(auth)` (a FastAPI dependency = `require_role(auth, "owner")`).
- Every owner-scoped store module (`customer_store.py`, `vehicle_store.py`, `estimate_store.py`, `work_order_store.py`, `invoice_store.py`, `payment_store.py`, `notification_store.py`, `customer_history_store.py`, `dashboard_store.py`, `context_store.py`) had its data-scoping calls switched from `auth.user.id` to `effective_owner_id(auth)`. Actor/audit-trail fields (`created_by_user_id`, `actor_user_id`) were deliberately left as `auth.user.id` since those record who acted, not whose shop the data belongs to. `square_store.py` needed no change — it inherits scoping transitively through `invoice_store.py`'s already-scoped invoice lookup.
- `app/main.py`: new `OwnerAuthContextDep` (`Depends(require_owner_context)`) now gates all 38 business routes (customers, vehicles, customer history, estimates + approval-history, work orders, invoices + payments + Square push/refresh, notifications, dashboard summary). Deliberately still open to any authenticated role (not owner-gated): `/api/auth/login`, `/api/auth/logout`, `/api/auth/me`, the three `/api/context/*` routes, `/api/location/resolve`, `/api/chat`, and the three token-authenticated `/api/estimate-approval/*` routes (public, no session at all). Work orders are fully owner-gated for now even though the long-term plan is technician access to their own assigned work order — that requires `WorkOrder.assigned_technician_id`, which is sub-phase 2 scope, not this one.
- New `tests/test_role_isolation.py` (4 tests): a static test that introspects the live FastAPI route table (`main.app.routes[*].dependant`) to assert every `/api/*` route not on an explicit exclusion allowlist transitively depends on `require_owner_context` — this fails automatically if a future route forgets the gate, rather than relying on a hand-maintained list; an end-to-end `TestClient` test that inserts a technician `UserAccount` row directly (no provisioning endpoint exists yet) and drives real HTTP requests confirming the technician gets `403` on a representative sample of routes while the owner still gets `200`; a DB-level check-constraint rejection test; and an `effective_owner_id` orphan-technician (`shop_owner_id is None`) `403` test.
- Verification: `ruff format`/`ruff check .` clean, `pyright` 0 errors, `pytest -q` 214 passed (210 prior + 4 new in `tests/test_role_isolation.py`), `node --check app/static/app.js` clean.
- Live proof (2026-07-11): rebuilt the `backend` Docker image, applied `alembic upgrade head` against the real dev Postgres (confirmed `shop_owner_id`/FK/check-constraint/index all present via `psql \d user_accounts`), round-tripped `alembic downgrade -1` then `upgrade head` to confirm the migration is cleanly reversible, then created a synthetic technician account (`live-verify-tech`, password known only to this session, tied to the real owner's `shop_owner_id`) directly via the app's own `hash_password`. Real `curl` requests against `http://127.0.0.1:8000` confirmed: technician login succeeds and returns `role: "technician"`; `GET /api/auth/me` returns `200`; `GET /api/customers`, `/api/dashboard/summary`, `/api/work-orders` all return `403` with `"Your role does not have access to this action."`. The synthetic technician account was deleted afterward — nothing synthetic remains in the dev database.
- Independent review (`optimus-reviewer`, 2026-07-11): no correctness defects. Confirmed the `effective_owner_id`/actor-field split was applied correctly everywhere (checked every remaining `auth.user.id` call site), confirmed the `main.py` owner-gating exclusion list is correct and complete, confirmed `square_store.py` correctly needed no direct edit. One cosmetic note: `docs/context/PLANS.md`'s original sub-phase 1 file list named `square_store.py` as needing the swap when it didn't (fixed in this doc pass).
- Security review (`optimus-security-reviewer`, 2026-07-11): **PASS**, no cross-tenant isolation break, no authorization bypass, no fail-open behavior. One low-severity hardening recommendation, not exploitable today (no technician provisioning endpoint exists yet to construct the scenario): `effective_owner_id` doesn't verify `shop_owner_id` actually references a `role="owner"` row — a technician could theoretically be chained to another technician or self-referenced if a bug in future provisioning code allowed it. Recorded as an action item for sub-phase 2's technician-provisioning endpoint in `docs/context/KNOWN_ISSUES.md`; no fix needed against this diff since the bypass has no working exploit path yet.
- Not done / explicitly out of scope this sub-phase: technician provisioning endpoint, `Technician`/`WorkOrder.assigned_technician_id` tables, any technician-specific route (clock-in/out, own-work-order view). All sub-phase 2 scope per `docs/context/PLANS.md` — see below.
- **Merged to `main` via PR #17** (2026-07-11), originally committed as `d7f31eb` (bundled with sub-phase 0). Not deployed to staging.

## Phase 5.6 Sub-phases 3, 4, 6, 7 — Vendors + Parts, Service Desk, Diagnostics + Inspections, Reports (merged to main via PR #20)

- Owner instruction (2026-07-13, mid-session): "continue on the new functions coming soon... finish all phases and complete all" — build out the remaining disabled "Coming soon" nav stubs (Service Desk, Diagnostics, Inspections, Parts, Vendors) into real full-stack modules, following the Technicians-module pattern (`app/technician_store.py`) as the template, without stopping for approval before implementation. Scheduling and Reports were already handled in an earlier slice this session (Reports built from existing dashboard/invoice endpoints; Scheduling left as an explicit static placeholder describing the missing backend).
- Three new migrations, chained off `012_technicians`: `alembic/versions/013_vendors_parts.py` (`vendors`, `parts` — `parts.vendor_id` nullable FK, `SET NULL` on vendor delete, `CHECK (quantity_on_hand >= 0)`), `alembic/versions/014_intake_requests.py` (`intake_requests` — nullable FKs to `customers`/`vehicles` for post-conversion linkage, `SET NULL` on delete), `alembic/versions/015_diagnostics_inspections.py` (`diagnostic_findings`, `inspections` — both require a `vehicle_id`, `CASCADE` on vehicle delete; nullable FKs to `work_orders`/`technicians`, `SET NULL`). `inspections.items` is a JSON column (owner-defined checklist items: label/status/note), matching the existing `JSON` import already used elsewhere in `db_models.py`.
- Five new store modules mirroring `technician_store.py`'s exact shape (`_owner_query`, `effective_owner_id` scoping, normalization helpers, `model_fields_set`-based partial updates): `app/vendor_store.py`, `app/part_store.py` (validates `vendor_id` belongs to the same owner before assigning), `app/intake_store.py` (adds `convert_intake_request()` — creates a real `Customer` via the existing `customer_store.create_customer`, and optionally a `Vehicle` via `vehicle_store.create_vehicle` if make/model were supplied at conversion time; rejects double-conversion and hand-editing a converted request's status back), `app/diagnostics_store.py`, `app/inspection_store.py` (both validate `vehicle_id`/`technician_id` belong to the same owner; use hard delete rather than the soft-archive pattern used everywhere else, since nothing else references these rows and they're closer to log entries than persistent business records — a deliberate, disclosed deviation from the archive convention).
- New `app/main.py` routes, all owner-gated (`OwnerAuthContextDep`), same exception-mapping pattern as every other module (`NotFoundError`→404, `ConflictError`→409, `StoreError`→422, `SQLAlchemyError`→503): `/api/vendors`, `/api/parts`, `/api/intake-requests` (+ `/convert`), `/api/diagnostic-findings`, `/api/inspections`. Diagnostics/Inspections additionally expose `DELETE` (hard delete, `204`) since they don't use the archive pattern. **Superseded 2026-07-15 by Phase 6 Part D**: Diagnostics/Inspections now use the same archive convention as every other module (`DELETE` archives and returns `200` with an `*ArchiveResponse`, not a `204` hard delete) plus a new append-only audit-event log (`GET .../{id}/events`) and `created_by`/`updated_by`/`archived_by` tracking — see `docs/context/PLANS.md`'s Phase 6 Part D entry for full detail. The hard-delete deviation described in this paragraph and the next no longer reflects current behavior.
- Frontend: all 5 nav items converted from `is-disabled`/`aria-disabled`/"Soon"-badge stubs to real `data-view` destinations (Scheduling keeps its "Soon" badge — it's a placeholder, not a real module). New `#view-service-desk`, `#view-diagnostics`, `#view-inspections`, `#view-parts`, `#view-vendors` sections in `app/static/index.html`, each following the existing `.customers-layout` list+detail+form pattern (same as Customers/Vehicles) rather than the denser rail pattern used for Work Orders/Invoices, since none of these needed a three-column layout. Inspections' checklist uses a small client-side "add item" control (`state.inspections.draftItems`) rather than a fully dynamic repeating-field-group UI, to keep scope bounded. `app/static/app.js` gained ~900 lines: five `initializeX()`/`loadX()`/`renderXList()`/`selectX()`/`populateXForm()`/`submitXForm()` function sets, plus two shared dropdown-population helpers (`loadVehicleOptionsInto()`, `loadVendorOptionsInto()`) reused across Diagnostics/Inspections/Parts. Also removed a stray leftover from the prior UI-redesign slice: `.server-stack`/`.location-radar` decorative markup was cleaned from System bay but not My Day (caught by independent review, fixed, and covered by a new test assertion).
- Verification: `ruff format`/`ruff check .` clean, `pyright` 0 errors, `node --check app/static/app.js` clean, `git diff --check` clean. `pytest -q` 255 passed (233 prior + 22 new: `tests/test_vendors_and_parts_api.py`, `tests/test_service_desk_api.py`, `tests/test_diagnostics_and_inspections_api.py` — every module has an explicit cross-owner-isolation test, per this file's standing rule that isolation is a hard stop).
- Live proof (2026-07-13): rebuilt the `backend`/`worker` Docker images, restarted the full compose stack, ran `alembic upgrade head` against the real dev Postgres (confirmed all 5 tables via `psql \dt`), then round-tripped `alembic downgrade 012_technicians` → confirmed tables dropped → `alembic upgrade head` → confirmed re-created, proving clean reversibility. Real `curl` against `http://127.0.0.1:8000` confirmed `/health`/`/ready` healthy and all 5 new endpoints correctly `401` unauthenticated. A real Playwright browser against the same CSP-enforcing port confirmed all 5 new views render with zero non-401 console errors and zero CSP violations, using the same client-side `state.auth.authenticated = true` bypass technique as the prior UI-redesign slice (no real session cookie, no real API data) — **this is a real, disclosed limitation**: nothing in this slice was verified against a real authenticated session with real business data end-to-end, because no synthetic-owner-account creation path exists (only the first owner is bootstrapped from `.env`, which this session never read) and using the real owner's actual credentials wasn't done without the owner present. The 22 backend tests (which do exercise real route handlers end-to-end against a real database, just not through a browser) are the actual functional-correctness evidence for this slice, not the Playwright pass.
- Independent review (`optimus-reviewer`, 2026-07-13, run twice — once on the UI-redesign layer, once on the 5 new modules): first pass found and the same-session fix applied for the My Day decorative-markup leftover described above. Second pass found a real Medium-severity cross-tenant write-path isolation gap: `work_order_id` on Diagnostics/Inspections was accepted from the client and written straight through with no ownership check, unlike its sibling fields `vehicle_id`/`technician_id` which were both correctly validated — the DB-level FK only proves the work order *exists*, not that it belongs to the calling owner. Fixed same-session (`_validate_work_order()` added to both `app/diagnostics_store.py` and `app/inspection_store.py`, called on both create and update whenever `work_order_id` is set), with two new regression tests (`test_diagnostic_finding_rejects_cross_owner_work_order`, `test_inspection_rejects_cross_owner_work_order`) proving a caller can no longer link a finding/inspection to another owner's work order. Re-verified: full suite green (257 tests), Docker image rebuilt and redeployed with the fix. Everything else in that second pass (owner-scoping on every other new store function, the intake-conversion flow's use of existing customer/vehicle store functions rather than reimplementing them, exception-mapping consistency, no new inline scripts/styles, no duplicate ids, hard-delete safety on the two tables nothing else FKs into) verified correct with no further findings. The one low-severity, non-exploitable note (`part_store.py`'s vendor-name lookup didn't filter by owner) was also fixed same-session — `_to_read` now takes `auth` and filters the lookup by `effective_owner_id(auth)`, threaded through all 5 call sites.
- Not done / explicitly out of scope: purchase-order workflow for Vendors (only a vendor directory + parts linkage was built — the original disabled-stub tooltip mentioned "vendor/purchase-order," but no PO nav item or requirement was named explicitly, so a full PO lifecycle was treated as a follow-up rather than invented); a full docs/context/PLANS.md Phase 5.6 sub-phase checklist entry per sub-phase (this section covers all three sub-phases together rather than one entry each, given the scope already delivered in one pass); security review (only independent review was run at ship time, not the separate `optimus-security-reviewer` pass PLANS.md's standing checklist calls for — none of these 5 modules are role-split, all are strictly owner-only, which lowers the risk profile somewhat but doesn't fully substitute for the review). **Update (2026-07-17)**: all 5 of these modules, plus Reports (Phase 6 Part G) and Phase 6 Part H's own hardening code, have since had that `optimus-security-reviewer` pass — Diagnostics+Inspections, Reports, and Part H came back **PASS with no exploitable findings**; Vendors/Parts/Purchase-Orders/Part-Allocation and Service Desk each surfaced one real Low/Low-Medium finding, both fixed same-day. Full detail in `docs/context/KNOWN_ISSUES.md`'s Historical Resolved Issues. Every module in the codebase has now had a dedicated security review at least once.
- **Merged to `main` via PR #20** (2026-07-13). Not yet deployed to staging (see corrected Staging Prep section).

## Phase 5.6 Sub-phase 5 — Scheduling Module (merged to main via PR #23)

- Full spec supplied directly by the owner (2026-07-13): an `Appointment` model with a specific field list, availability rules (technician/bay overlap, travel buffer, working hours, schedule blocks, no-past-appointments), specific REST endpoints, a day/week frontend calendar, and specific test coverage. Built end-to-end in one pass, replacing the static `#view-scheduling` placeholder described in the sub-phases-3-5 section above.
- New migration `alembic/versions/016_scheduling.py`, chained off `015_diagnostics_inspections`: `bays` (owner-scoped, archivable), `working_hours` (per-technician recurring weekly rows — `day_of_week`/`start_minute`/`end_minute` stored as local-time-of-day integers, not UTC instants, since a "9am-5pm Monday" rule must not shift with DST), `schedule_blocks` (technician-only, bay-only, or shop-wide via both FKs nullable — `CHECK` at the model layer now forbids setting both, see the review-fixed bug below), `appointments` (all fields from the owner's spec plus `owner_user_id`; `technician_id` required, `bay_id` optional; `status` CHECK-constrained to the 6 specified values; `end_time > start_time` and `travel_buffer_minutes >= 0` CHECK constraints mirroring the ORM-level ones in `app/db_models.py`).
- New `app/scheduling_store.py`: the conflict/availability engine. `_validate_slot()` runs, in order: past-time rejection, working-hours enforcement (converts the appointment's UTC start/end to `America/Chicago` via `zoneinfo`, compares local day-of-week/minute-of-day against configured `WorkingHours` rows — **skipped entirely if a technician has zero rows configured**, treated as unrestricted rather than blocking everything by default), technician overlap (`_technician_conflicts` — two active appointments for the same technician conflict unless the gap between them is `>= max(buffer_A, buffer_B)`, correctly checked in both temporal directions), bay overlap (plain interval overlap, no buffer), and schedule-block conflicts (`_schedule_block_applies` — a block matches if it names that technician, that bay, or neither/shop-wide). All conflicts are collected and returned together as structured `AppointmentConflictDetail` objects (`code`/`message`/`conflicting_appointment_id`/`conflicting_schedule_block_id`) via a 409 response, not just a single message. `SELECT ... FOR UPDATE` locks the technician row (and bay row, if set) before conflict-checking on create/update/move, mirroring `app/payment_store.py`'s existing row-lock pattern for concurrency safety (unverified under real concurrent load — SQLite, used by the test suite, ignores `FOR UPDATE`; this is inspection-only evidence, flagged by the independent review as a known gap).
- New `app/main.py` routes, all owner-only (`OwnerAuthContextDep`, confirmed by the existing `tests/test_role_isolation.py` static audit with zero changes needed to that file): the 7 appointment endpoints from the spec (`GET`/`POST /api/appointments`, `GET`/`PATCH /api/appointments/{id}`, `POST .../move`, `POST .../cancel`) plus `GET /api/availability`, full CRUD for schedule blocks (as specified), and — **a disclosed scope addition beyond the literal spec** — full CRUD for bays and working hours (`/api/bays`, `/api/working-hours`), since the spec only asked for *read* endpoints on those two and there would otherwise be no way to configure them at all, making the feature unusable/untestable end-to-end.
- Frontend (`app/static/index.html` + `app/static/app.js` + `app/static/styles.css`): a day/week **agenda-style** calendar (list+detail+form pattern matching every other module in this app — not a pixel-positioned time grid, a deliberate simplification consistent with the spec's explicit allowance for "a move/reschedule dialog... acceptable for version one" instead of drag-and-drop). Day view groups by technician; week view groups by day, both sorted chronologically and merging appointments with schedule blocks in one timeline. Status-colored left-border + badge (tentative/confirmed/in_progress/completed/canceled/no_show all visually distinct; blocked time gets a dashed/hatched card). Create/edit uses the existing form-panel pattern; Move opens an inline sub-form (not a popup — no modal-dialog infrastructure exists anywhere else in this codebase, so one wasn't introduced); Cancel requires a reason via an inline sub-form. 409 conflict responses are unpacked into their individual `conflicts[].message` strings for the toast, not just the generic wrapper message. `shopLocalInputToUtcIso`/`utcIsoToShopLocalInput` convert `<input type="datetime-local">` values to/from UTC using `Intl.DateTimeFormat`'s `shortOffset` to derive the real America/Chicago offset at that instant (DST-aware); the code comments disclose it doesn't special-case the ~1-hour spring-forward/fall-back ambiguity window, a reasonable simplification.
- New `tests/test_scheduling_api.py` (23 tests): creation, end-before-start rejection, past-time rejection, technician overlap, bay overlap, travel-buffer conflict + clearance, working-hours enforcement, a dedicated DST test (`test_working_hours_are_dst_aware` — asserts the *same* 9am-local appointment lands at different real UTC hours in winter vs. summer, not just that both succeed), blocked-time enforcement, move + move-revalidates-conflicts, cancellation releasing the slot, double-cancel rejection, invalid customer/vehicle/technician/bay/work-order references (including vehicle-not-owned-by-customer), cross-owner isolation, editing-a-canceled-appointment rejection, the two review-driven regression tests below. One pre-existing test (`tests/test_official_ui.py::test_overview_dashboard_and_approval_queue_markup`) had its stale `nav-soon-badge` count assertion updated from `1` to `0`, since Scheduling was the last "Coming soon" stub.
- Independent review (`optimus-reviewer`, 2026-07-13) found and both were fixed same-session: (1) `ScheduleBlock` allowed setting both `technician_id` and `bay_id`, which `_schedule_block_applies`'s OR semantics would apply more broadly than the UI implied — fixed with a `model_validator` on `ScheduleBlockBase` (and the equivalent check in `update_schedule_block`) rejecting both being set; a block now targets a technician, a bay, or neither (shop-wide), never both. (2) The frontend hid the Cancel button for a `completed` appointment but the backend didn't enforce that — `cancel_appointment` now rejects canceling a completed appointment server-side too. Both got new regression tests (`test_schedule_block_rejects_both_technician_and_bay`, `test_cannot_cancel_completed_appointment`). Everything else reviewed (buffer math, DST handling, row-lock ordering, owner scoping on every FK validator, exception-ordering in routes, migration/ORM constraint parity, frontend/backend field-name contract) checked out correct against the actual passing test run.
- Security review (`optimus-security-reviewer`, 2026-07-13): **PASS, no findings.** Confirmed owner-scoping on every store function and FK validator, all 22 new routes owner-gated, no raw-SQL injection surface, consistent `escapeHtml()` usage across every new frontend render function, no cross-owner data in 409 conflict details, and no exception internals/secrets leaked in error responses.
- A live-UI bug was found (not by either review, but during this session's own Playwright verification) and fixed: the toolbar's date-range label and Day/Week active-button highlighting only updated on a *successful* fetch, so a failed load (e.g. session expiry) left the toolbar visually stuck on stale state — `renderSchedulingDateLabel()` now runs unconditionally at the start of `loadScheduling()`, confirmed fixed via a before/after screenshot comparison.
- Verification: `ruff format`/`ruff check .` clean, `pyright` 0 errors, `pytest -q` 278 passed (255 prior + 23 new), `node --check app/static/app.js` clean. Migration round-tripped against the real dev Postgres (`alembic upgrade head` → confirmed all 4 tables via `psql \dt` → `alembic downgrade 015_diagnostics_inspections` → confirmed dropped → `alembic upgrade head` → confirmed re-created). `curl` against the rebuilt `backend` container confirmed all new routes correctly `401` unauthenticated. A real Playwright browser against the live dev stack (nginx `:5173`, proxying to the rebuilt backend) confirmed the Scheduling view, create-appointment form, and week view all render correctly with the dark automotive design system intact, using the same client-side `state.auth.authenticated = true` bypass technique as every prior slice's live proof (no real session cookie exists, so every real API call still correctly returns `401` and the UI honestly shows "Schedule failed to load: Authentication required" / empty states rather than fake data) — same disclosed limitation as every other Phase 5.6 slice: no real authenticated end-to-end click-through with real business data has been done, since no synthetic-owner-provisioning path exists.
- Not done / explicitly out of scope: technician-facing schedule visibility (this module, like the rest of Phase 5.6, is strictly owner-only — a mobile mechanic's own crew currently has no way to see their own schedule outside the owner's view; a reasonable follow-up, not attempted here); drag-and-drop rescheduling (explicitly deferred by the owner's own spec in favor of the move dialog); customer-facing self-booking (no customer accounts exist in this system at all).
- **Merged to `main` via PR #23** (2026-07-13). Not yet deployed to staging (see corrected Staging Prep section). Confirmed still under active development (per this repo's own instructions: Scheduling changes are explicitly out of scope for any subsequent session unless separately requested).

## Job Estimator draft layout + collapsible evidence rail (merged to main via PR #21, PR #22)

- Owner report (2026-07-13): a completed/drafted estimate rendered squeezed into the narrow ~300px readiness-rail grid column instead of the main content area — root cause was a stray `.estimate-layout #result { grid-column: 2; }` CSS rule. Fixed (PR #21) by moving `#result` to `grid-column: 1` (the main track) and hiding the input form once `renderEstimate()` populates a draft, so the finished estimate now uses the full main-content width; the sidebar rail (`app/static/index.html`'s `.estimate-side-rail`) was made collapsible (toggle button, state persisted in `localStorage`) so it can be tucked away for more room. Verified with a headless Playwright pass against the static files directly (no backend needed for a pure layout check): pre-draft form/rail widths, post-draft full-width result, collapse/expand toggle behavior, and the "Start another" reset path.
- Follow-up owner request (2026-07-13, same day): remove the decorative "Estimate readiness" gauge/checklist box entirely — it wasn't tied to any real signal. Fixed (PR #22) by deleting the `.readiness-card`/`.readiness-gauge` markup and its now-dead CSS, renaming the rail's remaining toggle/panel to "Evidence standard" (the one real card left in the rail).
- Both changes are pure `app/static/{index.html,app.js,styles.css}` edits — no backend, schema, or API surface touched. `tests/test_official_ui.py` (13 tests) still green after both changes.
- Git-coordination note: PR #22 was isolated by hand from a much larger, concurrent, uncommitted diff on the same three files (a different Claude session was simultaneously building the Scheduling frontend in the same shared worktree) using a manually-constructed single-hunk patch applied with `git apply --cached`, so only the readiness-box removal was staged/committed — the other session's in-flight Scheduling frontend work was left completely untouched and was committed/merged separately and correctly afterward (PR #23). See the Operational Snapshot's concurrent-session note above and `docs/context/KNOWN_ISSUES.md`.
- **Merged to `main` via PR #21 and PR #22** (2026-07-13). Verified live on staging as of this doc's "Last verified date": PR #21 (full-width layout + collapsible rail) **is** live on `https://staging.optimus-os.com` (confirmed via a served-HTML marker check — `side-rail-toggle` present); PR #22 (readiness-box removal) and PR #23 (Scheduling) are **not yet** live on staging (`readiness-card` and `nav-soon-badge` markers still present in the served HTML) — the droplet needs another `git pull --ff-only origin main` + `scripts/optimusctl.sh update`/`migrate` pass. This is a deployment-lag gap, not a merge problem: both PRs are confirmed merged into `main` via `gh pr list --state merged`.

## Phase 5.6 Sub-phase 2 — Technicians Module (merged to main via PR #17)

- Full spec in `docs/context/PLANS.md`'s Phase 5.6 section, sub-phase 2 entry. This is the first sub-phase where a technician account can actually be created and logged into — sub-phase 1 was auth plumbing with no provisioning path.
- New migration `alembic/versions/012_technicians.py`: `technicians` table (owner-scoped staff records — identity, contact, employment, qualifications, plus a nullable unique `user_account_id` FK linking to a provisioned login) and `technician_time_entries` table (clock in/out log, with a **DB-level partial unique index** — `clock_out_at IS NULL` — enforcing at most one open shift per technician even under a request race). `work_orders` gained `assigned_technician_id` (nullable FK, `ON DELETE SET NULL`) and `is_comeback` (boolean). Mirrored in `app/db_models.py`'s `Technician`/`TechnicianTimeEntry` ORM models and the two new `WorkOrder` columns.
- New `app/technician_store.py`: full CRUD (create/get/update/archive/list) mirroring `customer_store.py`'s exact pattern (same `effective_owner_id` scoping, same search/pagination shape) — explicitly the template every later Phase 5.6 sub-phase's CRUD module reuses without re-describing it. Plus `provision_login()` (creates the technician's `UserAccount`, re-validates the sub-phase-1 security-review-mandated "shop_owner_id resolves to a real owner" check as defense-in-depth even though the route itself is owner-gated), `clock_in()`/`clock_out()` (technician self-service, backed by the DB partial unique index for race safety), and `get_my_technician_profile()` (assembles the "My Day" view's data).
- `app/work_order_store.py::_work_order_query` gained a `db` parameter and a technician-scoping branch: for `role == "technician"`, it resolves the caller's own `Technician` row and filters `assigned_technician_id == <that id>`, or matches zero rows (not unfiltered access) if no `Technician` profile is linked yet. New `assign_technician()` (owner-only, validates the target technician belongs to the same owner) plus `is_comeback` support in `update_work_order()`.
- `app/main.py`: new `OwnerOrTechnicianAuthContextDep` (wraps new `app/auth.py::require_owner_or_technician_context`). Work orders partially opened to technicians: `GET /api/work-orders`, `GET /api/work-orders/{id}`, `POST /api/work-orders/{id}/status`, `POST /api/work-orders/{id}/notes`. Everything else stays strictly owner-only: all Technician CRUD + provisioning routes, `PATCH /api/work-orders/{id}` (general edit), and the new `POST /api/work-orders/{id}/assign-technician`. Three new technician-self routes: `GET /api/technicians/me`, `POST /api/technicians/me/clock-in`, `POST /api/technicians/me/clock-out` — registered *before* `/api/technicians/{technician_id}` so FastAPI's route-matching doesn't swallow `/me` into the int path parameter.
- `tests/test_role_isolation.py` rewritten (not just extended) for the new reality: `test_every_business_route_is_owner_gated` became `test_every_business_route_is_role_gated_as_expected`, checking every `/api/*` route against three buckets (not-role-gated-at-all, owner-or-technician, else-strictly-owner-only) and flagging both a missing gate *and* an unintended widening. The end-to-end test now asserts `/api/work-orders` returns `200` + empty `items` for a technician with no linked `Technician` profile (not a flat `403`).
- New `tests/test_technicians_api.py` (15 tests): CRUD + isolation + pagination, provisioning success/double-provisioning/duplicate-username rejection, a direct `provision_login()` call with a hand-built chained-technician `AuthContext` proving the defense-in-depth owner-check actually rejects it, clock-in/out double-action rejection, an `hourly_cost`-exclusion assertion on the self-service response, and a full approved-estimate → work-order → assignment → technician-sees-only-their-own-work-order end-to-end flow.
- Frontend (`app/static/index.html` + `app/static/app.js`): new `#view-technicians` (list/detail/form CRUD + login-provisioning sub-panel, mirroring `#view-customers`) and `#view-my-day` (technician landing: clock in/out, assigned-work-order count, recent time entries). Nav items got `data-owner-only`/`data-technician-only` markers (desktop sidebar + mobile bottom nav) toggled by new `applyRoleNavVisibility()`; post-login and post-reload routing branches on role; periodic notification/approval-queue badge polling gated to owner role only. Work-order detail view gained an owner-only "Assign technician" panel (dropdown + `is_comeback` checkbox).
- Verification: `ruff format`/`ruff check .` clean, `pyright` 0 errors, `pytest -q` 230 passed (214 prior + 15 new in `tests/test_technicians_api.py` + 1 new `hourly_cost`-exclusion assertion), `node --check app/static/app.js` clean.
- Live proof (2026-07-11): rebuilt the `backend` Docker image, applied `alembic upgrade head` (confirmed `technicians`/`technician_time_entries` schema and the partial unique index via `psql`), round-tripped `alembic downgrade -1` then `upgrade head` to confirm reversibility, then ran a full real Playwright browser walkthrough against the rebuilt container: synthetic owner logs in → creates a technician → provisions its login → confirms the work-order assignment dropdown is populated → logs out; synthetic technician logs in → confirms nav shows only Work Orders/My Day/System (not Overview/Customers/Technicians/etc.) → clocks in → clocks out → confirms the assign-technician panel is hidden → confirms a direct `/api/customers` fetch from its own browser session returns `403`. Zero console errors, zero CSP violations. All synthetic data deleted afterward.
- Independent review (`optimus-reviewer`, 2026-07-11): found one real Medium-High functional bug (see "fixed same-day" below); everything else (the `_work_order_query` signature migration and all 4 call sites, route registration order, the full `main.py` gating matrix, `test_role_isolation.py`'s rewritten three-bucket logic, DB race safety, FK/cascade behavior) verified correct. Also flagged the pre-existing untracked `optimusOS/` stray directory as a repo-hygiene risk (unrelated to this diff, already documented in `SESSION_HANDOFF.md` as "leave alone").
- Security review (`optimus-security-reviewer`, 2026-07-11): found one real Medium finding (`hourly_cost` leak, see below) and one Low/informational robustness note (provisioning race status code, see below); everything else (provisioning integrity, cross-tenant/cross-technician work-order isolation, clock in/out DB-level race safety, fail-closed behavior on an unlinked `Technician` profile, secrets handling, route registration order) confirmed correct with no findings.
- **Two real findings fixed same-day, both re-verified live** (full detail in `docs/context/KNOWN_ISSUES.md`'s Historical Resolved Issues): (1) a technician losing their My Day landing on page reload (frontend routing bug — `navigate()`'s URL-rewrite to `/` after login meant the reload-path role check never fired); (2) `hourly_cost` (an owner-only wage field) leaking into the technician's own `GET /api/technicians/me` response (fixed via a new `TechnicianSelfRead` model that excludes it). A third, minor, non-security robustness item (`provision_login`'s username-race returning the wrong HTTP status) was fixed at the same time.
- Not done / explicitly out of scope this sub-phase: Parts Inventory + Vendors, Service Desk, Scheduling, Diagnostics + Inspections, Reports (all later Phase 5.6 sub-phases).
- **Merged to `main` via PR #17** (2026-07-11), alongside sub-phase 0+1. Not deployed to staging.

## Work Order Slice

- Work Order backend status: implemented with canonical PostgreSQL persistence in `work_orders`, `work_order_status_events`, and `work_order_notes`, all owner-scoped and added by Alembic migration `007_work_orders`.
- Work Order conversion rule: only an `approved` estimate with an approved revision can convert; idempotency is enforced by a database unique constraint on `(estimate_id, estimate_revision_id)`.
- Work Order lifecycle status set: `pending_requirements`, `ready_to_schedule`, `scheduled`, `in_progress`, `waiting_for_parts`, `waiting_for_approval`, `completed`, `cancelled`.
- Work Order transition rule: `waiting_for_approval` exists in the enum only; no route transitions into it yet.
- Payment-plan rule: estimates approved with `split_payment` or `two_month_plan` convert to `pending_requirements`; transition to `ready_to_schedule` is blocked until both `deposit_received` and `authorization_confirmed` are true.
- Work Order notes rule: notes are append-only and explicitly labeled `internal` or `customer` for visibility separation.
- Work Order API surface:
  - `POST /api/estimates/{estimate_id}/work-order`
  - `GET /api/work-orders`
  - `GET /api/work-orders/{id}`
  - `PATCH /api/work-orders/{id}`
  - `POST /api/work-orders/{id}/status`
  - `POST /api/work-orders/{id}/notes`
- Work Order frontend status: static frontend now includes a `Work orders` navigation view with list/search/status filter, detail rendering, status controls, notes, and a `Create work order` action from an approved saved estimate.

## Invoice Slice

- Invoice backend status: implemented with canonical PostgreSQL persistence in `invoices` and `invoice_line_items`, added by Alembic migration `008_invoices`.
- Invoice creation rule: only a `completed` work order can generate an invoice; completion idempotently creates one owner-scoped `draft` invoice per work order via a unique constraint on `work_order_id`.
- Invoice snapshot rule: invoice rows store customer-safe customer and vehicle snapshots plus customer-facing line items and totals derived from the approved estimate revision used by the completed work order.
- Invoice status rule: Phase 2 only actively uses `draft` and `issued`; `partially_paid`, `paid`, `overdue`, and `void` remain reserved for Phase 3 payment tracking and later hardening.
- Invoice document rule: HTML and PDF outputs are rendered from invoice-safe fields only; internal estimate research reasoning, unselected competitor options, raw request overrides, and other forbidden internal fields are excluded.
- Invoice API surface:
  - `GET /api/invoices`
  - `GET /api/invoices/{id}`
  - `POST /api/invoices/{id}/issue`
  - `GET /api/invoices/{id}/html`
  - `GET /api/invoices/{id}/pdf`
- Invoice frontend status: static frontend now includes an `Invoices` navigation view with list/search/status filter, detail rendering, issue controls, HTML/PDF document actions, and a bridge from completed work orders into their generated invoice.

## Payment Tracking Slice (Phase 3)

- Payment backend status: implemented with canonical PostgreSQL persistence in `invoice_payments` and `payment_schedules`, added by Alembic migration `009_payments`, in a new `app/payment_store.py` module (an intentional, reviewed deviation from the plan's original file layout — `record_payment`/`void_payment` live separately from `app/invoice_store.py` and import its shared helpers read-only).
- Append-only ledger rule: `invoice_payments` rows are never updated or deleted. Voiding inserts a negative-amount reversal row (`reversal_of_payment_id` set); a DB-level `UniqueConstraint` on `reversal_of_payment_id` blocks double-voiding even under a request race, and a `CheckConstraint` enforces the amount-sign/reversal invariant.
- Derived-field rule: `invoice.status`, `total_paid`, `balance_due`, and `is_overdue` are always recomputed server-side from non-voided payments plus `due_at`, fresh at every read (`_to_read`); the physical `invoices.status` column is only a best-effort cache updated on payment-write paths. Client-supplied financial status is never accepted.
- Money-math rule: all payment/balance arithmetic uses `Decimal` (`_money()`, `ROUND_HALF_UP`, quantized to `0.01`); float conversion happens only at the Pydantic response boundary.
- Overpayment rule: rejected with `422`, no tolerance; corrections are void + re-record only. The overpayment check locks the invoice row (`SELECT ... FOR UPDATE`, Postgres-only — a no-op under SQLite tests) for the duration of `record_payment` so concurrent submissions against the same invoice serialize instead of both passing a stale balance check.
- Deposit rule: a payment with `applies_to=deposit` on a payment-plan work order (`split_payment`/`two_month_plan`) flips `deposit_received` to `true` in the same transaction if not already set; voiding that payment does **not** auto-revert it (documented limitation — the owner can flip it back via the existing `PATCH /api/work-orders/{id}`).
- Payment schedule rule: generated exactly once on `issue`, using an explicitly-flagged **placeholder default even split** (100% for `pay_in_full`; 50/50 for `split_payment`; roughly-thirds deposit/30-day/60-day for `two_month_plan`), with any rounding remainder absorbed into the final row so amounts always sum exactly to `invoice_total`. The real deposit/installment percentage split is still unconfirmed business policy — see `docs/context/BUSINESS_RULES.md`.
- No card/bank/payment-authorization fields exist anywhere in the new schema — `method_label` is free text only, by design, so there is nothing to leak.
- Payment API surface:
  - `POST /api/invoices/{id}/payments`
  - `POST /api/invoices/{id}/payments/{payment_id}/void`
  - `GET /api/invoices` / `GET /api/invoices/{id}` responses now also include `total_paid`, `balance_due`, `is_overdue`, `payments[]`, and `schedule[]`
- Payment frontend status: the existing invoice detail panel (not a parallel billing surface) now shows balance/paid summary, an overdue badge, payment history with void controls, a read-only schedule list, and a record-payment form.
- Explicitly out of scope for this slice (per owner decision 2026-07-08): any Square or other external payment processor, any live/billable payment API call, and any change to Square/external scheduling or the existing Work Order status lifecycle — Square integration is deferred to a distinct future phase with its own design/approval pass.

## Hardening Slice (Phase 4)

- Status: **Closed** (owner decision, 2026-07-09). Four of the roadmap's six deliverables landed as permanent test/script files; two (fresh-volume E2E flow, failure drills) were satisfied via a one-time manual live proof rather than new permanent automated Docker/E2E infrastructure. The owner has explicitly accepted the one-time proof as sufficient to close Phase 4 — a permanent automated fresh-volume E2E/failure-drill artifact remains unbuilt and is not planned unless separately requested.
- New files: `tests/test_isolation_sweep.py`, `tests/test_idempotency_audit.py`, `tests/test_document_exposure_scan.py`, `scripts/scan_logs_for_secrets.py`.
- Investigated and ruled out a suspected defect in `app/auth.py::get_current_auth_context` (Postgres-down error handling): it already wraps its DB access in `try/except SQLAlchemyError` returning a sanitized `503`; confirmed by code reading, a live drill, and independent + security review. No code changed.
- One-time live proof (2026-07-08) against an isolated `optimus_e2e` Docker Compose project (separate volumes/ports, same real hardened image): fresh-volume migration from empty to `009_payments (head)`; full Customer(seeded)→WorkOrder→Completion→Invoice→Payment→`paid` flow via real HTTP; clean secret-log scan; Redis-down/up and Postgres-down/up drills both showed sanitized responses and clean recovery with no backend restart; full-stack restart preserved all data; dev stack/volumes confirmed untouched throughout; clean teardown.
- Out of scope, unchanged: Square/external payment or scheduling integration, live/billable OpenAI calls.
- Merged to `main` via PR #7 (Phase 3, commit `423192b`) and PR #8 (Phase 4, commit `c920891`) on 2026-07-09.

## Staging Prep (Phase 5, in progress)

- Status: **real staging infrastructure is live and reachable.** DigitalOcean droplet (`137.184.102.247`) running the stack via `docker-compose.yml` + `ops/docker-compose.staging.yml`; domain `optimus-os.com` registered through Cloudflare with `staging.optimus-os.com` proxied (Flexible SSL, Always Use HTTPS on). `https://staging.optimus-os.com/health` and `/ready` confirmed reachable end to end from outside the droplet.
- Droplet commit state (re-verified 2026-07-13 via `curl`/served-HTML marker checks against `https://staging.optimus-os.com` — not assumed from a prior doc claim, since the `36b861b` figure previously recorded here was many merges out of date and not re-checked): staging is running a commit **after PR #21** (the estimator full-width layout fix — `side-rail-toggle` marker present in served HTML) but **before PR #22 and PR #23** (readiness-box removal and Scheduling — `readiness-card` and `nav-soon-badge` markers both still present). `/health` and `/ready` both return `200`. This means at least one droplet update has happened more recently than this doc previously reflected, but at least two more merged PRs are still pending deployment. Re-run the droplet update runbook (`git pull --ff-only origin main` → `scripts/optimusctl.sh update` → `migrate` → `health`/`ready`) to bring it fully current. HSTS confirmed live on the public domain 2026-07-09 (not re-checked this pass).
- Code/process changes made and rehearsed against the local dev stack: HSTS header added to `app/main.py` (env-gated on `frontend_origin` being https, same pattern as the existing Secure-cookie check); `scripts/optimusctl.sh` gained `restore` (scratch-database-only, with a live-db-name guard sourced from `.env`, a reserved-system-db denylist, and an identifier-shape check), `rollback` (image-tag-based, sudo-aware), and `migrate-down` subcommands, and (2026-07-09, `7d665c8`) `COMPOSE_OVERRIDE_FILE` support so the staging droplet keeps its public port binding through every subcommand.
- A separately reported frontend bug (invoice detail buttons — "Open work order/HTML/PDF" — staying disabled after being opened from a work order) was root-caused to an async race condition in `app/static/app.js` and fixed with a selection-version guard; live-tested with Playwright network interception to deterministically prove both the bug and the fix. See `docs/context/SESSION_HANDOFF.md` for full detail.
- Two secret-exposure incidents occurred and were disclosed this session (a `docker compose config` dump, and a mangled-terminal-paste incident) — see `docs/context/KNOWN_ISSUES.md` and `SESSION_HANDOFF.md`.
- Independent review + security review completed on the earlier local-prep changes; four real findings fixed same-day: a sudo-docker regression risk in the new rollback code, a live-database-guard reading the wrong environment source, a missing PostgreSQL reserved-name denylist, and a doc PR/phase mislabeling. See `docs/context/KNOWN_ISSUES.md` for detail.
- Not yet started: separate staging PostgreSQL/Redis with distinct credentials from any other environment beyond what's already set, secrets-store handling beyond `.env` on the host, external `/health`+`/ready` monitoring/alerting, disk-space alerting.

## Verification Status

- Formatting and static checks passed on 2026-07-08:
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run ruff format .`
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run ruff check .`
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run pyright`
  - `node --check app/static/app.js`
- Full automated test suite passed on 2026-07-08:
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`
- Targeted Work Order test coverage passed on 2026-07-08 in `tests/test_work_orders_api.py`, covering the roadmap categories for auth rejection, approved conversion, non-approved rejection, idempotency, revision preservation, copied labor/parts/totals, cross-user isolation, valid transitions, invalid transitions, payment prerequisite blocking, note visibility separation, cancellation, restart persistence, and sanitized storage failures.
- Docker and migration checks passed on 2026-07-08:
  - `docker compose config -q`
  - `docker compose build backend worker`
  - `docker compose up -d backend worker frontend`
  - `docker compose exec -T backend alembic heads` → `008_invoices (head)`
  - `docker compose exec -T backend alembic upgrade head`
  - `docker compose exec -T backend alembic current` → `008_invoices (head)`
- Non-billable live Work Order proof passed on 2026-07-08 against the rebuilt Docker stack:
  - seeded an approved estimate fixture and converted it through the real UI
  - exercised `pending_requirements -> ready_to_schedule -> scheduled -> in_progress -> waiting_for_parts -> in_progress -> completed`
  - added both internal and customer-visible notes
  - restarted `backend` and `worker` and verified persistence after restart
  - proved live cross-user isolation with a second synthetic owner: `GET /api/work-orders/{id}` and duplicate conversion attempts returned `404`
  - proof result summary: `estimate_id=63`, `work_order_id=2`, `final_status=completed`, `notes_count=2`, `status_events=7`
- Independent review completed on 2026-07-08. Follow-up fixes shipped for:
  - blocked payment-plan transitions no longer appear as available next statuses
  - "Open estimate" from a work order now fetches the estimate when it is not already cached client-side
  - adding a note refreshes the parent work order `updated_at` so list recency matches execution activity
- Security review completed on 2026-07-08 with no new findings in the Work Order diff.
  - Reviewed surfaces: owner scoping on all work-order queries/routes, cross-user access behavior, append-only status/note writes, payment-plan prerequisite gating, and frontend HTML rendering/escaping for work-order content
  - Result: no auth bypass, no customer-document exposure regression, no raw-storage error leak, and no XSS introduced in the new work-order UI rendering path
- Phase 2 automated verification passed on 2026-07-08:
  - full suite still green after invoice changes
  - `tests/test_invoices_api.py` covers completed-work-order invoice generation, duplicate-completion idempotency, totals preservation, issue stamping, HTML/PDF forbidden-field exclusion, cross-user isolation, historical snapshot persistence, restart persistence, and sanitized storage failures
  - follow-up regression coverage now proves a failed completion-time invoice create rolls back the work-order status update instead of persisting a `completed` work order without an invoice
  - follow-up regression coverage now also proves non-canonical fee items are included in `fees_total`, long/multiline invoice line items survive HTML/PDF generation, and cross-user denial applies to invoice issue/PDF routes in addition to list/detail/html
- Phase 2 post-review fixes shipped on 2026-07-08:
  - work-order completion and invoice creation now succeed or roll back atomically in the same transaction
  - invoice HTML now uses the shipped `/static/invoice.css` asset so the rendered document remains styled under the app's existing `style-src 'self'` CSP
  - invoice `fees_total` now derives from all approved fee items instead of assuming only the three canonical fee codes
  - invoice line-item descriptions now use `Text` in the committed `008_invoices` schema/model instead of a narrower `String(240)` cap
  - PDF rendering now wraps long/multiline customer-visible content instead of truncating raw lines
  - invoice selection now re-renders the list so the active-row highlight stays in sync with the selected invoice
- Non-billable live invoice proof passed on 2026-07-08 against the rebuilt Docker stack:
  - completed a live work order into invoice generation
  - opened the invoice through the real owner UI from the work-order detail view
  - issued the draft invoice through the UI
  - retrieved live HTML and PDF document outputs with `200` responses and `%PDF-1.4` output prefix
  - retrieved `/static/invoice.css` with `200` and confirmed the invoice HTML references it
  - restarted `backend` and `worker` and verified the issued invoice still loaded with the same invoice number and PDF response
  - proved live cross-user isolation with a second synthetic owner receiving `404` on invoice access
  - proof result summary after the rebuilt-stack re-run: `estimate_id=69`, `work_order_id=6`, `invoice_id=4`, `invoice_number=INV-00004`, `final_status=completed`, `issued_status=issued`, `cross_user_status=404`
- Phase 2 security review completed on 2026-07-08 with no findings.
  - Reviewed surfaces: completion-triggered invoice creation, owner scoping on list/detail/document routes, HTML escaping, PDF field narrowing, invoice snapshot contents, and invoice document exposure boundaries
  - Result: no auth bypass, no cross-user leak, no internal-research leak in document output, and no new unsafe HTML rendering path
- Phase 2 independent re-review completed on 2026-07-08 with no remaining findings after the fixes.
- Phase 3 automated verification passed on 2026-07-08:
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check app/db_models.py app/invoice_store.py app/main.py app/models.py app/payment_store.py alembic/versions/009_payments.py tests/test_payments_api.py` → clean (repo-wide `ruff format --check .` flags 4 pre-existing files unrelated to this diff — confirmed via `git stash` that the drift predates this session)
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run ruff check .` → all checks passed
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run pyright` → 0 errors
  - `env UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q` → 165 passed (16 new in `tests/test_payments_api.py`, all Phase 1/2 tests still green)
  - `node --check app/static/app.js` → OK
- Phase 3 Docker/migration verification passed on 2026-07-08:
  - `docker compose config -q`
  - `docker compose build backend worker`
  - `docker compose up -d backend worker frontend`
  - `docker compose exec -T backend alembic heads` → `009_payments (head)`
  - `docker compose exec -T backend alembic upgrade head`
  - `docker compose exec -T backend alembic current` → `009_payments (head)`
  - rollback rehearsal: `alembic downgrade 008_invoices` then `alembic upgrade head` → clean round-trip, back at `009_payments (head)`
- Phase 3 independent review completed on 2026-07-08: no correctness bugs, no regressions (`app/work_order_store.py` confirmed untouched), all 16 planned test cases present, cross-user isolation confirmed on both new routes. Two minor, accepted architecture-drift notes: payment logic landed in a new `app/payment_store.py` module rather than inside `invoice_store.py`, and `InvoicePaymentCreate.amount` has an added `le=1_000_000` upper bound not in the original plan text — both harmless and reflected in this doc.
- Phase 3 security review completed on 2026-07-08: no critical/high findings. One medium finding (overpayment check not race-safe against concurrent requests on the same invoice) was fixed same-day by adding a `SELECT ... FOR UPDATE` row lock in `record_payment`; full gates re-ran green after the fix.
- Non-billable live proof passed on 2026-07-08 against the rebuilt Docker stack (fixture-seeded estimate, no OpenAI call, owner/session constructed directly against the real DB without touching `.env`):
  - two-month-plan estimate → work order conversion → blocked premature scheduling (prereqs unmet) → deposit payment recorded → `deposit_received` flipped by the payment itself → invoice issued with a 3-row schedule (`Deposit`/`Installment 1`/`Installment 2`, summing exactly to `invoice_total` in Decimal) → status `partially_paid` → overpayment rejected → balance paid in full → status `paid`, balance `0` → deposit payment voided → status recomputed back to `partially_paid`, balance `186.31`, `deposit_received` unchanged (`true`) → double-void rejected
  - restarted `backend`/`worker` and confirmed identical state after restart: `invoice_status=partially_paid`, `total_paid=372.62`, `balance_due=186.31`, `payment_count=3`, `schedule_count=3`, `work_order_status=completed`, `deposit_received=true`
  - proved live cross-user isolation with a second synthetic owner: `GET /api/invoices/{id}`, `POST /api/invoices/{id}/payments`, and `POST /api/invoices/{id}/payments/{payment_id}/void` all returned `404`
  - proof result summary: `estimate_id=71`, `work_order_id=8`, `invoice_id=6`, `owner_user_id=12`

## Next Approved Implementation Phase

- Phase 1 is closed and merged to `main`.
- Phase 2 is closed and merged to `main`.
- Phase 3 is closed and merged to `main` (PR #7, `423192b`).
- Phase 4 is closed and merged to `main` (PR #8, `c920891`) — owner accepted the one-time live proof as sufficient for the fresh-volume E2E/failure-drill deliverables; no permanent automated version is planned unless separately requested.
- Phase 5 — Private Staging: infrastructure exists and is reachable, but the droplet is behind `main` by at least two merged PRs (#22, #23) as of this doc's verification pass — see the corrected Staging Prep section above. Separate staging Postgres/Redis credentials, secrets-store handling beyond `.env`, external `/health`+`/ready` monitoring/alerting, and disk-space alerting remain not started.
- Phase 5.6 (Operations Forms Modules & Multi-Role Authorization, all 8 sub-phases) is closed and merged to `main` (PR #17, #19, #20, #23) — every "Coming soon" nav stub is now a real module. See `docs/context/PLANS.md` for the per-sub-phase checklist and the disclosed deviations from the original spec (no Purchase Orders, no technician write-access to Diagnostics/Inspections, no `SavedReportRequest` tracking log on Reports). The originally-disclosed hard-delete-instead-of-soft-archive deviation on Diagnostics/Inspections was resolved by Phase 6 Part D (2026-07-15) — see below.
- Next approved phase: the owner's 10-phase production-readiness roadmap (CI enforcement, synthetic test-account provisioning, authenticated end-to-end Playwright coverage, Diagnostics/Inspections auditability, technician RBAC for those modules, Parts/Vendors purchase-order + allocation workflow, Reports completion, production security hardening, staging verification) — see `docs/context/PLANS.md`'s expanded Phase 6 for the tracked checklist. Scheduling is explicitly out of scope for this roadmap per the owner's instruction.
