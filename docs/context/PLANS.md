# Plans

Purpose: durable phase checklist for OptimusOS from the verified Estimate Approval slice through to a controlled customer pilot. This is the single "where are we" reference — read it before re-deriving a roadmap.
Information owner: repository maintainers (roadmap authored 2026-07-07).
Read when: starting any new slice, or checking overall project sequencing.
Update when: a phase's acceptance criteria are met, or the sequence changes.
Last verified date: 2026-07-15 (Phase 6 Part F closed — Part Allocation slice added, implemented and live-verified including a genuine concurrent-thread race test on `allocate_part`, on branch `agent/claude/part-allocations`, not yet merged; Purchase Orders slice merged via PR #36; Phase 6 Part E merged via PR #35, Part D merged via PR #34, Part J merged via PR #32/#33; all Phase 5.6 sub-phases remain confirmed merged to `main` via `gh pr list --state merged` and a clean-checkout gate run — 278 tests passing as of the last full-repo pass, prior to this session's additions).
Relevant sources: `docs/context/CURRENT_STATE.md`, `docs/context/KNOWN_ISSUES.md`, `docs/context/SESSION_HANDOFF.md`, `AGENTS.md`, `CLAUDE.md`.

## Standing rules (apply to every phase below)

- PostgreSQL is the source of truth. The context manager is assistive memory only — never store full customer, vehicle, estimate, work order, invoice, payment, approval-token, or payment-authorization records there.
- Never disclose supplier cost, markup, margin, internal pricing logic, approval tokens, session cookies, API keys, or raw database errors to customer-facing surfaces or logs.
- Every business record is scoped to the authenticated owner/user. Cross-user isolation is tested at every slice — a failing isolation test is a hard stop for the whole project, not just that slice.
- Every slice ships backend + frontend + tests + non-billable runtime proof + docs + a replaced `SESSION_HANDOFF.md`.
- One agent owns one branch/worktree at a time. The implementing agent never grades its own slice — an independent review (different agent) is required before a slice is considered done.
- Do not weaken Estimate Approval validation to make downstream work easier.
- No live OpenAI calls, commits, pushes, merges, or deploys without explicit current-turn approval.

## Phase checklist

### Phase 0 — Freeze and back up Estimate Approval
- [x] Estimate Approval live-verified against a real OpenAI call (2026-07-06).
- [x] Verified files committed (`ce39561` "verify: complete estimate approval live proof").
- [x] Branch renamed `feat/vehicle-management` → `feat/estimate-approval`.
- [x] Pushed to GitHub — `origin/feat/estimate-approval` confirmed at `ce39561`.
- [x] `AGENTS.md` / AI Coordination Pack committed locally on the Work Orders branch as `6d0c332` (`chore: add AI coordination pack`).
- [x] The local `feat/work-orders` branch remained local-only until explicit push approval was granted in the current session.
- [x] `SESSION_HANDOFF.md` now reflects the local branch reality instead of the stale estimate-approval branch.

**Acceptance:** commit SHA recorded, GitHub backup confirmed (local HEAD == origin HEAD), `SESSION_HANDOFF.md` current, Work Orders not started before backup is confirmed.

### Phase 1 — Work Orders
**Goal:** an approved estimate converts to a work order without re-entering customer, vehicle, labor, parts, or pricing data.

Branch: `feat/work-orders`, from `feat/estimate-approval` (only after Phase 0 fully checked off).

Key rules:
- Convert only from an `approved` estimate revision; conversion keyed by a DB-level unique constraint on `(estimate_id, revision_id)` so repeats return the existing row (idempotent by construction, not app-logic).
- Customer/vehicle context comes from the approved revision's existing snapshot — no new copies.
- Status transitions are server-controlled via one transition table; every transition recorded in an append-only `work_order_status_events` table.
- Payment-plan work orders start `pending_requirements` (gated on `deposit_received` / `authorization_confirmed` booleans, owner-settable only); non-payment-plan orders start `ready_to_schedule`.
- `waiting_for_approval` status exists in the enum for future change-order support but **nothing routes to it yet** — change orders are their own future slice, not part of Phase 1.
- No real payment processing.

Statuses: `pending_requirements`, `ready_to_schedule`, `scheduled`, `in_progress`, `waiting_for_parts`, `waiting_for_approval`, `completed`, `cancelled`.

API: `POST /api/estimates/{estimate_id}/work-order`, `GET /api/work-orders`, `GET /api/work-orders/{id}`, `PATCH /api/work-orders/{id}`, `POST /api/work-orders/{id}/status`, `POST /api/work-orders/{id}/notes`.

Frontend: list page, detail page (status controls, notes, status history, blocked-transition explanation), "Create work order" action from an approved estimate.

Required tests: unauthenticated rejection; approved-estimate conversion; draft/declined/awaiting-approval rejection; duplicate-conversion idempotency; approved revision preserved; labor/parts/totals copied correctly; cross-user isolation; valid status transitions; invalid status transitions; payment-plan prerequisite blocking; notes visibility separation; cancellation; restart persistence; sanitized dependency failures.

- [x] Implemented in source: backend store/routes/models, Alembic migration `007_work_orders`, static frontend list/detail/status/notes flow, and targeted regression tests.
- [x] Verified complete:
  - all 14 roadmap test categories covered in `tests/test_work_orders_api.py`
  - full suite green
  - `ruff` / `pyright` green
  - Docker rebuild green
  - Alembic script head and live DB head both at `007_work_orders`
  - non-billable live browser/API proof of convert → status walk → notes → restart persistence
  - explicit live cross-user isolation proof
  - independent review completed and follow-up fixes applied
  - security review completed with no findings
  - context docs updated to the current local branch state

**Acceptance:** all endpoints live; all 14 test categories pass; full suite stays green; ruff/pyright clean; Docker rebuild + migration `007_work_orders` applied; non-billable browser proof of convert → status walk → notes → restart persistence; cross-user isolation proven at the API level; independent review + security review pass; docs + handoff updated. No live OpenAI call needed for this slice.

### Phase 2 — Work Completion and Invoice PDF
**Goal:** completing a work order generates a customer-facing Landon Motor Works invoice.

Branch: `feat/invoices`, from the merged/verified Phase 1 state.

- Migration `008_invoices`: `invoices` (owner-scoped, FK to work order + estimate revision, server-calculated totals snapshot), `invoice_line_items` (customer-facing values only — supplier cost/markup never populate these columns).
- Completion of a work order idempotently creates a `draft` invoice (unique constraint on `work_order_id`); an explicit "issue" action moves it to `issued` and stamps `due_at`.
- Build the customer-facing document as server-rendered **HTML first**, reusing the narrow-model exposure pattern from `EstimateApprovalPublicView`; verify all content/exclusion tests against the HTML; add PDF rendering as a second layer over the same template once HTML is proven. If this slice runs long, split PDF rendering into its own follow-up mini-slice rather than letting Phase 2 sprawl.
- PDF library is a new dependency touching the deployment image — flag it explicitly in review, don't slip it in.
- Statuses: `draft`, `issued`, `partially_paid`, `paid`, `overdue`, `void` — the payment-driven transitions (`partially_paid`/`paid`/`overdue`) are stubbed until Phase 3 and must be documented as such, not implemented early.

Required tests: invoice generated on completion; duplicate completion does not duplicate invoice; totals preserved; PDF generated; PDF excludes forbidden internal fields (supplier cost, markup, margin, internal notes, and any other policy-forbidden field); cross-user isolation; historical snapshot persistence; restart persistence.

- Implemented in source: backend invoice store/routes/models, Alembic migration `008_invoices`, owner-facing invoice UI list/detail/issue/document flow, and targeted regression tests in `tests/test_invoices_api.py`.
- Verified complete:
  - invoice generated on work-order completion
  - duplicate completion remains idempotent
  - totals and customer-safe line items preserved
  - HTML and PDF document outputs generated
  - forbidden-field exclusion checks pass for HTML/PDF output
  - cross-user isolation proven in tests and live proof
  - historical snapshot persistence proven after customer/vehicle updates
  - restart persistence proven in tests and live proof
  - full suite green
  - `ruff` / `pyright` green
  - Docker rebuild green
  - Alembic script head and live DB head both at `008_invoices`
  - non-billable live proof of completion → invoice issue → HTML/PDF retrieval → restart persistence
  - independent review completed and follow-up fixes applied for atomic completion/invoice creation and CSP-safe invoice styling
  - independent review follow-up also fixed full-fee aggregation, long-description schema safety, long/multiline PDF rendering, and invoice-list selection refresh
  - security review completed with no findings
  - independent re-review completed with no remaining findings
- [x] Commit/push the verified Phase 2 slice before starting Phase 3 (`85e9bce`, pushed to `origin/feat/invoices` on 2026-07-08)

**Acceptance:** all 8 test categories pass; PDF text-extraction test asserts forbidden fields never appear; idempotent completion; restart persistence; reviews pass; docs updated.

### Phase 3 — Payment Tracking
**Goal:** track invoice payments, deposits, installments, balances, and overdue state without live payment processing.

Branch: `feat/payment-tracking`.

- Migration `009_payments`: `invoice_payments` (append-only — amount, method label, recorded_at, note, `reversal_of_payment_id` for reversals; **no card/bank fields in the schema at all**, so there is nothing to leak), `payment_schedules` (installments for the two-month plan).
- Balance = server-side Decimal sum over non-voided payments; invoice status derived server-side, never client-supplied.
- Overdue computed against `due_at` with an injectable clock so tests don't sleep or fake system time.
- Overpayment: reject by default (explicit, tested); corrections go through void + re-record, never delete.
- Recording a deposit payment satisfies the linked work order's `deposit_received` prerequisite.

Required tests: full payment; partial payment; deposit; installments; overpayment rejection; void/reversal; invoice status updates; overdue calculation; cross-user isolation; restart persistence. Plus Decimal-precision regression tests — no float arithmetic anywhere in money paths.

- [x] Implemented in source: backend store (`app/payment_store.py`), models, Alembic migration `009_payments`, extended owner-facing invoice detail UI, and 16 targeted regression tests in `tests/test_payments_api.py`.
- [x] Owner decision recorded 2026-07-08: this phase is an internal append-only ledger only — no Square/external payment processor, no live/billable calls, no change to Square/external scheduling or the Work Order status lifecycle. Square is deferred to its own future phase. Payment-schedule percentage split uses an explicitly flagged placeholder (even default split) pending real business-rule confirmation.
- [x] Verified complete:
  - all 16 required test categories covered, full suite green (165 passed)
  - `ruff` / `pyright` green on the touched files and repo-wide
  - Docker rebuild green; Alembic script head and live DB head both at `009_payments`; downgrade/upgrade rollback rehearsed cleanly
  - non-billable live proof: deposit payment flips `deposit_received`, schedule generated once and sums exactly to `invoice_total` in Decimal, overpayment rejected, void recomputes balance/status without reverting `deposit_received`, double-void rejected, restart persistence confirmed, cross-user isolation returns `404` on both new routes
  - independent review completed: no correctness bugs, no regressions, `app/work_order_store.py` confirmed untouched; two minor accepted deviations documented (payment logic in its own module; an added upper bound on payment amount)
  - security review completed: one medium finding (overpayment check not race-safe) fixed same-day with a `SELECT ... FOR UPDATE` row lock; no other findings
  - context docs updated to reflect the current local branch state
- [ ] Commit/push the verified Phase 3 slice — **awaiting explicit commit/push approval**, not yet performed.

**Acceptance:** all listed test categories pass; full suite stays green; ruff/pyright clean; Docker rebuild + migration `009_payments` applied with rollback rehearsed; non-billable live proof of deposit/overpayment/void/overdue/restart/cross-user behavior; reviews pass; docs updated. Met in full except the commit/push step, which requires explicit owner approval.

### Phase 4 — Full Local MVP Hardening
**Goal:** prove the complete local flow end to end. Proof slice only — no new product features except fixes for what it finds.

- One automated, non-billable, deterministic E2E test: Customer → Vehicle → Estimate → Approval → Work Order → Completion → Invoice → Payments → paid, against the real Docker stack, **on a fresh volume + migrations** (not the long-lived dev database everything so far has run against).
- Second-owner isolation sweep across every record type created by the first user.
- Failure drills: Redis down, PostgreSQL down, full restart — all with sanitized errors and clean recovery.
- Full-log secret scan, formalized into a reusable script.
- Customer-document exposure scan across every customer-facing HTML/PDF surface.
- Idempotency audit: re-fire every conversion/completion/issue action twice, assert no duplicates.
- One paid, explicitly-authorized live estimate generation as the E2E entry point **only if approved that turn**; otherwise the seeded-estimate path stands in for it.

**Exit criterion:** everything above green in a single clean run from `docker compose up` on a fresh volume.

- [x] Second-owner isolation sweep: `tests/test_isolation_sweep.py` — one chain, one second-owner session, every record type; closes a real pre-existing gap (vehicle update/archive/list were never isolation-tested).
- [x] Idempotency audit: `tests/test_idempotency_audit.py` — repeated status transition, repeated issue, repeated full-payment request all proven non-duplicating.
- [x] Full-log secret scan script: `scripts/scan_logs_for_secrets.py` — reusable CLI, never prints matched text, covers OpenAI keys/generic secret labels/any credentialed connection URL/session-cookie value/approval-token-shaped strings.
- [x] Customer-document exposure scan: `tests/test_document_exposure_scan.py` — invoice HTML/PDF and the public estimate-approval view checked against a shared forbidden-marker list.
- [x] Fresh-volume E2E test + failure drills (Redis down, Postgres down, full restart): satisfied via a **one-time manual live proof** on 2026-07-08 against an isolated `optimus_e2e` Docker Compose project, not a permanent committed automated test. Evidence in `docs/context/SESSION_HANDOFF.md`. **Owner accepted this as sufficient on 2026-07-09** — a permanent automated version is not planned unless separately requested.
- [x] No live/billable OpenAI call made — seeded-estimate path used, as allowed.
- Independent review and security review both completed 2026-07-08 on the four new files; two minor issues found and fixed same-day (secret-scan regex gap, docstring honesty about test overlap). No findings on the (ruled-out) suspected `app/auth.py` defect.
- [x] Committed and merged to `main` via PR #8 (`c920891`) on 2026-07-09.

**Phase 4 is closed.**

### Phase 5 — Private Staging
Branch: `ops/staging`, from `main` at `c920891`.

- Separate host/environment; separate PostgreSQL + Redis with distinct credentials; synthetic data only, no real customers.
- Private domain + HTTPS, HSTS, secure cookies flipped on (currently `false` locally — verify it's env-driven).
- Secrets injected from environment/secrets store, never committed.
- Migration strategy as an explicit deploy step with a rehearsed downgrade.
- Backups: nightly `pg_dump`, **restore actually rehearsed once** into a scratch database.
- Monitoring on `/health` + `/ready`, error-log alerting, disk-space alerting.
- Rollback rehearsed once (previous image tag retained).
- Decide *where* staging will live during Phases 1–3, so Phase 5 doesn't stall on unmade infrastructure decisions.

**Progress (2026-07-09):**

- [x] Secure cookies confirmed already env-driven: `settings.frontend_origin.lower().startswith("https://")` in `app/auth.py` — no code change needed, just confirmed by reading the code.
- [x] HSTS header added to `app/main.py`'s `security_headers` middleware, same env-gating pattern as the Secure cookie flag; regression tests in `tests/test_security_headers.py`.
- [x] Migration downgrade formalized as an explicit deploy step: `scripts/optimusctl.sh migrate-down <revision>`, rehearsed once against the dev stack (`009_payments` → `008_invoices` → back to head).
- [x] Backup/restore rehearsed: `scripts/optimusctl.sh restore <dump-file> [target-db]` added (restores into a scratch database only, with an identifier-shape check, a live-database-name guard read from `.env` — not the ambient shell — and a PostgreSQL reserved-name denylist). Rehearsed once: `backup` → `restore` into `optimus_os_restore_check`, row counts confirmed matching the live DB across `user_accounts`/`customers`/`invoices`/`invoice_payments`.
- [x] Rollback mechanism added and rehearsed once: `scripts/optimusctl.sh rollback` (retags `:previous` back to `:latest`, sudo-aware). Rehearsed by deliberately breaking the backend (retagged to a dummy image), confirming `/health` failed, then running `rollback` and confirming full recovery.
- [x] Independent review + security review completed on all of the above; findings (sudo-docker regression risk, live-db-guard env-source bug, missing reserved-db-name denylist, a doc PR/phase mislabeling) all fixed and re-verified. See `docs/context/KNOWN_ISSUES.md`.
- [x] Infrastructure decision made (ADR-011, `docs/context/DECISIONS.md`): staging will run on a DigitalOcean droplet, domain registered through Cloudflare (also solves HTTPS termination via Cloudflare's proxy).
- [x] Owner created the DigitalOcean droplet (`137.184.102.247`) and registered `optimus-os.com` through Cloudflare; stack deployed at `/opt/optimus-server`; `https://staging.optimus-os.com/health` + `/ready` verified reachable end to end (2026-07-09). HSTS confirmed live on the public domain.
- [x] `scripts/optimusctl.sh` `COMPOSE_OVERRIDE_FILE` support added (`7d665c8`) so droplet operations keep the staging port binding.
- [x] **Droplet redeployed 2026-07-10** (owner-authorized direct SSH execution): `ops/staging` had already merged into `main` via PR #11 on GitHub (branch deleted after merge, same pattern as PR #10), so the droplet fast-forwarded `main` `36b861b`→`b38a811` (clean, no conflicts) instead of switching branches. `COMPOSE_OVERRIDE_FILE` set in droplet `.env`; `scripts/optimusctl.sh update` rebuilt and restarted; `status` confirmed frontend at `0.0.0.0:80->80/tcp` (override held); external curl from this machine confirmed `selectionVersion` present in the served `app.js` through Cloudflare — invoice-button fix is live on `https://staging.optimus-os.com`.
- [ ] **Owner action still pending**: one full browser login on staging with the rotated password, and the manual invoice-button repro click-through (browser-only checks, not agent-performable).
- [ ] Not started: separate staging PostgreSQL/Redis with distinct credentials, secrets-on-host beyond `.env`, `/health`+`/ready` external monitoring/alerting, disk-space alerting.

### Phase 5.5 — Feature slice: estimate cleanup, customer history, notifications, Square sandbox — SHIPPED

Owner-approved 2026-07-09. Implemented by **Claude** on branch `agent/claude/notify-history-square` on 2026-07-10 (the owner's /goal directive required same-session delivery). Merged into `main` via PR #12 + #13 on GitHub (branch deleted after merge, same pattern as prior staging PRs). Deployed to the staging droplet and verified live on `https://staging.optimus-os.com` same day. Full spec in `docs/context/SESSION_HANDOFF.md`.

- [x] Slice 1 — retired transient `POST /api/estimate` (every estimate the UI creates is persisted with approve/decline tracking); 3 tests ported.
- [x] Slice 2 — `GET /api/customers/{id}/history` aggregator + customer-detail history panel (estimates with approved/declined status, work orders, invoices with live balance/overdue).
- [x] Slice 3 — `notifications` table (migration `010_notifications_square`, includes Square invoice columns) + owner feed API + Notifications tab with unread badge and 60s poll; in-transaction hooks at all seven status-change producers.
- [x] Slice 4 — Square Invoices sandbox integration (config-gated `square_configured`; production structurally unreachable; Square never writes the local payment ledger).
- [x] Gates: ruff/pyright/pytest (200 passed) /node clean; alembic 009↔010 round-trip rehearsed on the compose Postgres; live curl proofs (auth gates, served assets, real schema).
- [x] Independent review completed (no CRITICAL; three IMPORTANT findings fixed/documented — client-leak fix, pre-existing transaction gap recorded in KNOWN_ISSUES, coverage gap carried; unique Square-id index added). Gates re-run green.
- [x] Committed, pushed, and merged into `main` (PRs #12/#13 on GitHub).
- [x] **Local Square sandbox smoke test completed 2026-07-10** with real owner-created sandbox credentials, against the real Square sandbox API (no stubs): built a full customer→estimate→approved→completed-work-order→issued-invoice chain via direct store calls (non-billable — no OpenAI call), pushed it to Square, got back a real Square invoice id + live pay link, confirmed refresh works, and confirmed the local payment ledger stays untouched (Square reported the invoice UNPAID; local `total_paid`/`balance_due`/payment-row-count all correct and independent of Square's state). Found and the owner fixed one real config bug: `SQUARE_LOCATION_ID` had been set to the Square **Application ID** by mistake — corrected to the real sandbox location id from `GET /v2/locations`. Two minor non-blocking findings also surfaced: Square's live validator rejects `.test`-TLD emails (our own fixture-seeding convention, not a real-customer risk) and requires E.164 phone format (our customer records store free-text phone) — both would currently surface as a generic 502, not a defect, just an opportunity for friendlier error messages later.
- [x] **Deployed to the staging droplet and verified externally 2026-07-10**: `git pull --ff-only origin main` (clean fast-forward), `scripts/optimusctl.sh update` (rebuild+restart, port binding held at `0.0.0.0:80`), `scripts/optimusctl.sh migrate` (alembic 009→010 applied, confirmed via `alembic current`). External curl checks from outside the droplet confirmed `/health` now reports `square_configured`/`square_environment`, the served `app.js` contains all three new feature markers, and `index.html` contains the Notifications tab. Staging's own `.env` has no Square credentials, so `square_configured: false` there — expected; Square is proven working only against the local dev stack so far.
- [ ] Optional: add Square sandbox credentials to the staging droplet's `.env` + restart if the owner wants Square live on staging too (separate step, not yet done).
- Out of scope: Square live/production, webhooks, email/SMS channels, background jobs.

### Phase 5.6 — Operations Forms Modules & Multi-Role Authorization — CLOSED, all 8 sub-phases merged to `main` (PR #17, #19, #20, #23)

Owner-directed 2026-07-11, planned via `/plan` on branch `agent/claude/landing-page-redesign` (same branch as the Overview dashboard slice above). Source of truth for every module's fields: `/home/dejake/Downloads/Landon_Motor_Works_Operations_Forms.xlsx` (one sheet per module). Replaces the 8 disabled "Coming soon" nav stubs shipped with the Overview dashboard (Service Desk, Diagnostics, Inspections, Scheduling, Technicians, Parts, Vendors, Reports) with real, working modules, one phase at a time — each phase gets its own branch-local commit, gates, and independent review before the next starts, same discipline as Phases 1–4 above.

Owner decisions locked in before planning:
- Technicians get **real logins** (not just data records) — this requires OptimusOS's first genuine multi-role permission system, built first as its own sub-phase since every existing store module currently scopes data by `owner_user_id == auth.user.id` under a strict single-role isolation model.
- Vendors/PurchaseOrders normalized into two related tables (not the spreadsheet's flat one-row-per-PO-with-repeated-vendor-info layout).
- Full roadmap planned now, dependency-ordered; implemented one sub-phase at a time.

Also bundled into this same branch, already done: removed "Talk to Optimus" from the sidebar/mobile nav (chat stays reachable via the Overview "Direct command" panel's quick-prompts, which already `navigate("chat")` — no functionality lost).

**Sub-phases 0, 1, and 2 are implemented, live-verified, and independently + security reviewed (all passed) as of 2026-07-11 — full detail in `docs/context/CURRENT_STATE.md`'s "Phase 5.6 Sub-phase 0 & 1" and "Phase 5.6 Sub-phase 2" sections. Merged to `main` via PR #17.**

**Sub-phases 3, 4, 6, and 7 were built 2026-07-13 on `agent/claude/shop-management-ui`, each with real disclosed deviations from this plan's original scope (Purchase Orders not built, technician write-access not built, normalized `InspectionItem`/`Confidence`-enum reuse not built, `SavedReportRequest` tracking log not built — see each numbered entry below and `docs/context/CURRENT_STATE.md`'s "Phase 5.6 Sub-phases 3, 4, 6, 7" section for exactly what shipped instead). Independent review passed (one real bug found and fixed same-session); no dedicated `optimus-security-reviewer` pass has been run on these four sub-phases specifically, unlike sub-phases 1-2 and 5. **Merged to `main` via PR #20 (2026-07-13).**

**Sub-phase 5 (Scheduling) was built 2026-07-13, same branch, in a separate session from sub-phases 3/4/6/7.** Full `Appointment`/`Bay`/`WorkingHours`/`ScheduleBlock` data model, availability/conflict engine (technician overlap, bay overlap, configurable travel buffer, DST-aware America/Chicago working-hours enforcement, schedule-block conflicts), all 7 specified appointment endpoints plus `GET /api/availability`, full CRUD for schedule blocks (as specified) and — a disclosed scope addition beyond the literal spec, since otherwise the feature would be unconfigurable — full CRUD for bays and working hours too (the spec only asked for read endpoints on those). Day/week agenda-style calendar UI (not a pixel-positioned grid — a deliberate simplification, matching the spec's allowance for a move/reschedule dialog instead of drag-and-drop). 23 tests including a DST-crossing test that asserts real UTC-hour differences between winter/summer local appointment times. Independent review found and both bugs were fixed same-session: (1) a `ScheduleBlock` could be created with both `technician_id` and `bay_id` set, which the conflict engine treated as OR/broader-than-intended — now rejected at the model layer (a block targets a technician or a bay, not both); (2) the backend didn't block canceling a `completed` appointment even though the frontend hid that button — now enforced server-side too. Security review: **PASS, no findings.** Full detail in `docs/context/CURRENT_STATE.md`'s "Phase 5.6 Sub-phase 5" section.

**Sub-phase order (dependency-driven):**

1. **Multi-role authorization foundation — DONE 2026-07-11.** New migration adds `UserAccount.shop_owner_id` (nullable self-FK, `NULL` for owners, set to the shop owner's id for technicians) plus a `role IN ('owner','technician')` check constraint. `app/auth.py::effective_owner_id(auth)` (returns `auth.user.id` for an owner, `auth.user.shop_owner_id` for a technician) is now the single scoping call in every store module (`customer_store.py`, `vehicle_store.py`, `estimate_store.py`, `work_order_store.py`, `invoice_store.py`, `payment_store.py`, `notification_store.py`, `customer_history_store.py`, `dashboard_store.py`, `context_store.py`; `square_store.py` needed no direct edit since it inherits scoping transitively through `invoice_store.py`). New `require_role(auth, *allowed)` and `require_owner_context(auth)` route-level gates, applied to all 38 business routes in `app/main.py`. Technician permission boundary for v1 is enforced as fully owner-gated for now (technicians can log in but every business route 403s) since the "own assigned work orders" carve-out needs `WorkOrder.assigned_technician_id`, which is sub-phase 2 scope — see sub-phase 2 below. **Security review pass completed 2026-07-11: PASS, no blocking findings** — full detail in `docs/context/CURRENT_STATE.md`. One hardening item deferred to sub-phase 2's provisioning endpoint (see below), tracked in `docs/context/KNOWN_ISSUES.md`.
2. **Technicians — DONE 2026-07-11.** `app/technician_store.py`, new `Technician` + `TechnicianTimeEntry` tables (the latter with a DB-level partial unique index enforcing at most one open clock-in per technician), new `WorkOrder.assigned_technician_id` + `WorkOrder.is_comeback` columns. Owner-only CRUD + login provisioning (`POST /api/technicians/{id}/provision-login`, which re-validates the sub-phase-1-mandated `shop_owner_id`-resolves-to-a-real-owner check as defense-in-depth); technician self-service clock-in/out (`POST /api/technicians/me/clock-in|out`) and own-profile view (`GET /api/technicians/me`, deliberately excludes the owner-only `hourly_cost` wage field via a dedicated `TechnicianSelfRead` model — a security-review finding, fixed same-day). New `#view-technicians` (list/detail/form, same pattern as `#view-customers` — this sub-phase is the template CRUD pattern every later sub-phase below reuses without re-describing it) plus `#view-my-day` for technician-role sessions, with role-based nav visibility (`data-owner-only`/`data-technician-only` + `applyRoleNavVisibility()`) and role-based post-login/reload routing. Work orders carved open for technicians (own-assigned-only): `GET /api/work-orders`, `GET /api/work-orders/{id}`, `POST /api/work-orders/{id}/status`, `POST /api/work-orders/{id}/notes` now use a new `OwnerOrTechnicianAuthContextDep`, scoped in `work_order_store._work_order_query(db, auth)` via the technician's own linked `Technician.id`; a new owner-only `POST /api/work-orders/{id}/assign-technician` route lets the owner assign/unassign. **Independent + security review both PASS 2026-07-11**, two real findings fixed same-day (see `docs/context/CURRENT_STATE.md`'s "Phase 5.6 Sub-phase 2" section for full detail): a technician losing their My Day landing on page reload (frontend routing bug), and `hourly_cost` leaking into the technician's own `/api/technicians/me` response (now excluded).
3. **Parts Inventory + Vendors** (paired) — **PARTIALLY DONE, merged to `main` via PR #20.** Built: `Vendor` and `Part` tables (owner-scoped CRUD, `Part.vendor_id` nullable FK), `Part.unit_cost`/`unit_price` fields exist but the dashboard COGS follow-up described below was **not** started. **Deviation from this plan as originally written:** `PurchaseOrder` and `PartAllocation` were not built — this delivered a vendor directory + parts inventory with a reorder-threshold flag, not a purchase-order lifecycle. Full detail in `docs/context/CURRENT_STATE.md`'s "Phase 5.6 Sub-phases 3-5" section.
4. **Service Desk** (intake) — **PARTIALLY DONE, merged to `main` via PR #20.** Built: `IntakeRequest` table + a "convert" action, owner-only. **Deviation from this plan as originally written:** the convert action creates a `Customer` (+ optional `Vehicle` if make/model were supplied) via the existing customer/vehicle store functions, not a pre-filled `create_estimate` call — an owner still has to open the Job Estimator separately afterward. No `ServiceDeskIntake` table name was used (named `IntakeRequest` instead, tracked in `intake_requests`).
5. **Scheduling** (appointments) — **DONE 2026-07-13, same branch.** `Appointment`, `Bay`, `WorkingHours`, `ScheduleBlock` tables (migration `016_scheduling`); `app/scheduling_store.py` conflict engine covering technician overlap, bay overlap, travel buffer, working hours (DST-aware, skipped if a technician has zero configured hours), and schedule blocks; owner-only routes for appointments (list/get/create/patch/move/cancel), availability, schedule blocks, plus bay/working-hours CRUD (a disclosed scope addition beyond the literal endpoint list, needed to make bays/hours configurable at all). Day/week agenda-style frontend (list+detail+form pattern, not a pixel-grid). See sub-phase-5 addendum above for the two bugs an independent review found and fixed.
6. **Diagnostics + Inspections** (paired) — **PARTIALLY DONE, merged to `main` via PR #20.** Built: `DiagnosticFinding` and `Inspection` tables, owner-only CRUD (hard delete, not soft-archive — a deliberate deviation from every other module's convention, see `CURRENT_STATE.md`). **Deviations from this plan as originally written:** (1) not writable by the assigned technician on their own work order — both modules are strictly owner-gated (`OwnerAuthContextDep`), no `OwnerOrTechnicianAuthContextDep` carve-out was added; (2) `DiagnosticFinding` does not reuse the estimator's existing `Confidence` enum; (3) `Inspection` checklist items are stored as a JSON column on the `inspections` row rather than a normalized `InspectionItem` child table.
7. **Reports.** — **PARTIALLY DONE, merged to `main` via PR #20.** Built: a read-only Reports view presenting revenue/work-order/invoice-status/balance summaries sourced from the existing `/api/dashboard/summary` and `/api/invoices` endpoints — no new backend endpoint or table. **Deviation from this plan as originally written:** no `SavedReportRequest` table — this is a live dashboard-style view, not the recurring-report request/tracking log the plan described. Payment activity, technician time, and commission reports are explicitly out (documented in the view itself as needing new backend aggregation endpoints that don't exist).

**Required tests per sub-phase:** full CRUD + owner isolation (same pattern as every existing slice); from sub-phase 1 onward, also a cross-*role* isolation sweep (technician session gets `403`/empty on every owner-only route; sees only their own assigned work). Sub-phase 1 additionally reruns the *entire* existing test suite after the mechanical `effective_owner_id` swap, since a missed call site would silently regress an existing cross-user isolation guarantee.

**Verification per sub-phase:** same gate sequence as every slice on this branch — `ruff format`/`ruff check .`, `pyright`, `pytest -q`, `node --check app/static/app.js`, then a live Playwright check against the rebuilt `backend` container (the CSP-enforcing surface) under a synthetic seeded account, deleted afterward. No live OpenAI calls needed for any of this.

**Acceptance (per sub-phase):** backend + frontend + tests + non-billable live proof + docs updated + independent review passed, before the next sub-phase starts — sub-phase 1 additionally requires a security review pass. Nothing committed, pushed, or deployed without separate explicit owner approval, same as every other slice in this roadmap.

- [x] Sub-phase 0 — remove "Talk to Optimus" from nav. (2026-07-11, merged via PR #17)
- [x] Sub-phase 1 — multi-role authorization foundation + security review. (2026-07-11, merged via PR #17; security review PASS)
- [x] Sub-phase 2 — Technicians. (2026-07-11, merged via PR #17; independent + security review PASS, 2 findings fixed same-day)
- [x] Sub-phase 3 — Parts Inventory + Vendors. (2026-07-13, merged via PR #20; Purchase Orders/Gross-Profit-dashboard follow-up not built — now tracked as Phase 6 Part D below)
- [x] Sub-phase 4 — Service Desk. (2026-07-13, merged via PR #20)
- [x] Sub-phase 5 — Scheduling. (2026-07-13, merged via PR #23; independent + security review PASS, 2 findings fixed same-day)
- [x] Sub-phase 6 — Diagnostics + Inspections. (2026-07-13, merged via PR #20; hard-delete deviation resolved by Phase 6 Part D; no-technician-access deviation now tracked as Phase 6 Part E below)
- [x] Sub-phase 7 — Reports. (2026-07-13, merged via PR #20; full reporting buildout now tracked as Phase 6 Part G below)

### Phase 6 — Production Readiness

Owner-directed expansion (2026-07-13): with Phase 5.6 closed, work continues toward a stable, production-ready platform. **Scheduling is explicitly out of scope for every part below** — do not build, redesign, replace, or modify it; only avoid breaking its existing integration points. Each part below ships as its own branch/PR with the standard gate sequence (`ruff format --check`, `ruff check`, `pyright`, `pytest`, `node --check`, plus Alembic upgrade/downgrade and Docker build checks where migrations or images are touched) and an independent review before merge, same discipline as every phase above. Nothing is committed, pushed, merged, or deployed without explicit current-turn owner approval; no billable OpenAI calls without separate explicit approval for that exact run.

**Part A — CI enforcement. DONE, merged to `main`.** Replaced the previously handoff-only GitHub Actions setup (`.github/workflows/ai-coordination.yml` only validated the AI handoff doc — confirmed 2026-07-13, no test/lint/build gate ran in CI before this) with a new `.github/workflows/ci.yml`, three jobs on every PR and push to `main`: (1) `lint-typecheck-test` — `ruff format --check`, `ruff check`, `pyright`, `pytest`, `node --check app/static/app.js`, `git diff --check` against the PR base; (2) `migrations` — a real Postgres 16 service container, confirms a single linear Alembic head, upgrades from a clean database, then a downgrade/upgrade round-trip; (3) `docker-compose-integration` — validates `docker compose config -q` for both the base and staging-overlay compose files (placeholder `.env.example` values only, output always suppressed with `-q` so nothing resolved is ever printed), builds the backend/worker image via `docker compose build backend worker`, boots the full stack, polls `/health`+`/ready`, applies migrations inside the running container, and runs the existing `scripts/scan_logs_for_secrets.py` against all four services' logs. Every command in every job was hand-validated against real local Docker/Postgres containers before being committed to the workflow file, not just written from the spec. **Branch protection is not enabled on `main`** — confirmed via `gh api repos/.../branches/main/protection` returning `404 Branch not protected` on 2026-07-13. Recommendation, not yet actioned (repo-settings changes need separate explicit owner approval): require the `lint-typecheck-test`, `migrations`, and `docker-compose-integration` status checks before merge, require PRs before pushing to `main`, and consider requiring at least one approval once more than one human/agent regularly pushes here.

**Part B — Synthetic test-account provisioning. DONE, merged to `main`.** A test-only path to mint synthetic owner/technician accounts for automated and local E2E use. New migration `017_synthetic_test_accounts` (`user_accounts.is_synthetic_test_account`, indexed) + `app/test_support_store.py` + four new `/api/test-support/*` routes (`POST synthetic-owner`, `POST synthetic-technician`, `DELETE synthetic-accounts/{id}`, `DELETE synthetic-accounts` sweep). Double-gated — both `OPTIMUS_TEST_ACCOUNT_PROVISIONING=true` *and* a non-`"production"` `app_env` are independently required (neither alone is sufficient); every route returns a bare `404` when disabled rather than a `403`, so the routes don't reveal they exist in any real deployment that hasn't explicitly opted in (off by default everywhere, including local dev and CI). Random credentials (`secrets.token_urlsafe`), never the real owner's, reused via the existing `technician_store.create_technician`/`provision_login` functions rather than reimplementing technician creation. Cleanup refuses to delete anything not flagged `is_synthetic_test_account` (can never delete a real account, even by guessing/iterating ids) and explicitly deletes any linked technician row rather than relying solely on the database's `ON DELETE CASCADE` — see the SQLite-vs-Postgres FK-enforcement gap this surfaced, in `docs/context/KNOWN_ISSUES.md`. Verified: 8 new tests in `tests/test_test_support_api.py` (disabled-by-default, disabled-in-production, real owner provisioning + real login, real technician provisioning + real login, rejects attaching to a non-synthetic owner, per-account cleanup + cascade, refuses to delete a real account, sweep cleanup) plus a live proof against a real Postgres container over real HTTP (provision owner → provision technician → real `/api/auth/login` as both → delete owner → confirm both rows gone from Postgres → sweep-cleanup two more owners → confirm zero rows). Alembic upgrade/downgrade round-tripped against real Postgres. This directly closes the gap every Phase 5.6 sub-phase's handoff has flagged: *no real authenticated end-to-end browser proof exists for any Phase 5.6 module* — every "live Playwright proof" so far used a client-side `state.auth.authenticated = true` bypass against an unauthenticated session, not a real login. Part C can now use this for real logins instead.

**Part C — Authenticated Playwright E2E suite. FIRST INCREMENT DONE (core repair workflow), merged to `main`; newer-modules and security-behavior coverage NOT yet built.** Permanent tests (not one-off proofs) using Part B's real synthetic accounts and real API calls, no frontend-state bypass.

- **Built**: `tests/e2e/` — a new, separate test tree (excluded from the default fast `pytest` run via `norecursedirs`, run in its own CI job `e2e-core-workflow`). `tests/e2e/conftest.py`'s `live_server` fixture boots a real, throwaway Postgres container and a real `uvicorn` process running the actual FastAPI app against it (not a `TestClient`, not mocked state), with synthetic provisioning enabled; `synthetic_owner`/`synthetic_technician` fixtures provision and clean up real accounts via Part B's routes. `tests/e2e/test_core_workflow.py::test_core_repair_workflow_end_to_end` drives a real Chromium browser (via `pytest-playwright`, already a declared but previously-unused dependency) through: real login (real session cookie, not injected `state.auth`) → create customer → create vehicle → open a seeded estimate → send for approval → **approve via the real public approval link in a separate, unauthenticated browser context** (proves the token-based flow independent of the owner's session) → refresh to see the real approved status → convert to work order → walk `ready_to_schedule → scheduled → in_progress → completed` (real status-transition table, not a mock) → the atomically-created draft invoice → issue it → record a real payment → assert the real server-computed `status == "paid"` and `balance_due == 0.0` → assert no unexpected browser console errors. Every assertion is against a real HTTP response from the real backend.
- **One real, deliberate exception, disclosed rather than hidden**: estimate *creation* through the real UI form triggers a real, billable OpenAI research call. Rather than making that call or bypassing the estimate step's realism, `tests/e2e/seed.py::seed_ready_estimate` persists a complete, real `ready`-status estimate directly via the ORM, reusing this repo's own existing deterministic non-billable research fixture (`scripts/seed_estimate_approval_fixture.py::build_valid_response`, already established for this exact purpose). Every step *after* that point (opening it, approval, conversion, completion, invoicing, payment) is 100% real UI + real API, matching the same "seeded-estimate path" precedent already accepted for Phase 4's closure.
- **A real UI race condition found and fixed, not worked around**: `page.select_option()` on `#work-order-next-status` and `#vehicle-customer-id` doesn't wait for the *specific* option value to exist — only for the `<select>` element itself — and the work-order status form additionally re-renders a second time from a fire-and-forget list refresh shortly after each transition, which could silently reset the selection back to empty before the click landed (the app then no-ops with a toast rather than erroring, which is why it manifested as a timeout waiting for a network response that never fired, not a failed assertion). Fixed with an explicit "select, verify the value actually stuck, retry within a bounded attempt count" pattern rather than a blind sleep. Verified stable across repeated local runs after the fix (previously flaky roughly 1 run in 3-4 before it).
- **Not yet built** (tracked as follow-up, same file/pattern to extend): the Phase 5.6 "newer modules" checklist (vendor/part/reorder, Service Desk intake→conversion, diagnostic finding, inspection, technician My Day/clock-in-out/isolation), Reports against real seeded values, and the security-behavior checklist (401s, cross-role/cross-owner denial, no CSP violations, no internal cost/margin in customer-facing documents). Technician assignment specifically was also not exercised in this first increment (the core workflow completed without ever assigning a technician to the work order, since assignment isn't a hard prerequisite for the status walk) — worth adding to the same test or a follow-up one.
- Verified: `ruff format --check`/`ruff check .` clean, `pyright` 0 errors, default `pytest -q` still 286 passed (E2E tests correctly excluded from that run), `node --check` clean. The E2E suite itself: found flaky roughly 1 run in 3-4 with only the first race fix applied (the work-order status transition); after applying the same retry-and-verify pattern to the vehicle-customer selection too, 4 consecutive clean local runs. Given browser-timing races are never provably eliminated by a handful of runs, treat this as "hardened, not proven infallible" — re-open if it flakes again in CI. New CI job `e2e-core-workflow` runs it for real in GitHub Actions (Playwright Chromium installed via `playwright install --with-deps`, Docker-in-Docker for the Postgres container — same primitive every other CI job already uses).

**Part D — Diagnostics/Inspections auditability. DONE, awaiting merge (2026-07-15).** Replaced hard delete with archive semantics matching every other module's convention (customers, vehicles, vendors, parts, technicians, bays).
- Migration `019_diag_inspection_audit` (25-char revision id — the first attempt, `019_diagnostics_inspections_audit`, was 33 chars and exceeded Alembic's own `alembic_version.version_num` `VARCHAR(32)` column, caught by a live proof against real Postgres, not static review): adds `is_archived`/`archived_at`/`created_by_user_id`/`updated_by_user_id`/`archived_by_user_id` to `diagnostic_findings` and `inspections`, plus two new append-only tables `diagnostic_finding_events` and `inspection_events` (`event_type` CHECK IN `created`/`updated`/`archived`, `actor_type` CHECK IN `owner`/`technician` — the latter is forward-compatible with Part E below, which is not yet built, so every event recorded today is `actor_type=owner`).
- `DELETE /api/diagnostic-findings/{id}` and `DELETE /api/inspections/{id}` keep the same method/path (matching `DELETE /api/customers/{id}`'s existing archive-under-the-hood convention) but now archive instead of hard-deleting, returning a `*ArchiveResponse` instead of `204`. List routes gained an `archived: bool = False` filter param, matching `list_customers`'s exact convention. New `GET .../{id}/events` routes expose the append-only audit trail. Archiving is idempotent (no error re-archiving), matching `archive_customer`.
- Frontend: "Delete" buttons renamed to "Archive" (ids renamed to `diagnostics-archive`/`inspections-archive`, matching `customer-archive`/`vendors-archive`/`parts-archive`), a "Show archived" toggle added to both modules (matching `customers-archived-only`/`vendors-archived-only`/`parts-archived-only`), and an Active/Archived badge added to the detail view.
- Verified: 6 new tests added to `tests/test_diagnostics_and_inspections_api.py` (archive-and-relist, owner-scoped archive rejection, and created/updated/archived event-log assertions — one set per module), replacing the 2 old hard-delete tests; full suite green; ruff/pyright/`node --check` clean; migration upgrade/downgrade round-tripped cleanly against real Postgres with a single linear head confirmed via `alembic heads`; full live HTTP proof over a real running server with a real synthetic-owner login (Part B) — create → update → list active (excludes it after archive) → archive via `DELETE` → list `archived=true` (includes it) → direct `GET` by id still works (data isn't destroyed) → `GET .../events` returns `[created, updated, archived]` with correct `actor_name`/`actor_type` — for both diagnostic findings and inspections.
- No dedicated `optimus-security-reviewer` pass was requested for this part specifically; an `optimus-reviewer` correctness/regression pass was run instead (see review notes before merge).
- **Not in scope for Part D** (tracked separately): technician read/write access to these modules is Part E, not yet built; the audit-event log currently has no dedicated frontend history-viewer UI (backend + API only) — a deliberate scope boundary, since the primary requirement was data-integrity/paper-trail, not a new UI surface.

**Part E — Technician workflow for Diagnostics/Inspections. DONE, awaiting merge (2026-07-15).** Both modules were strictly `OwnerAuthContextDep`-gated with no technician carve-out (a disclosed Phase 5.6 deviation); now technicians have read/write access scoped to their own assigned work orders, same pattern as `work_order_store.py`'s existing carve-out.
- `POST`/`GET` (list)/`GET` (detail)/`PATCH` on both `/api/diagnostic-findings` and `/api/inspections` changed from `OwnerAuthContextDep` to `OwnerOrTechnicianAuthContextDep`. `_owner_query` in both stores now scopes a technician session to `work_order_id.in_(select(WorkOrder.id).where(WorkOrder.assigned_technician_id == technician.id))` — deliberately not the row's own (client-settable) `technician_id` field, per the explicit "do not rely on the FK alone" instruction.
- `_validate_work_order` hardened for the technician actor: rejects a missing `work_order_id` outright (an unlinked finding/inspection would be permanently invisible to whoever created it as a technician), requires the work order to actually be assigned to that technician (not merely same-owner), and cross-checks that the finding/inspection's `vehicle_id` matches the linked work order's own vehicle — closing a gap where a technician could otherwise cite a valid assigned work order but an unrelated vehicle.
- `DELETE` (archive) and `GET .../events` (audit trail) deliberately stay `OwnerAuthContextDep` — archiving and audit history remain owner-only actions, matching how `assign-technician` stayed owner-only when work orders got their own technician carve-out. Frontend Archive buttons gained `data-owner-only="true"` so a technician session doesn't see a button that would just 403 (and a related pre-existing-pattern bug was fixed in the same change: `renderDiagnosticsDetail`/`renderInspectionsDetail` were directly overwriting that hidden state every time a record was selected — fixed by also checking `isTechnicianSession()`).
- **Not done / explicit scope boundary**: the "excluding supplier cost/internal labor cost/margin/owner notes/other-technician wage data" clause in this Part's original description does not apply to these two specific modules — neither `DiagnosticFinding` nor `Inspection` has any such field to redact (unlike `Technician.hourly_cost`, which is the precedent that clause was written against). No field-level redaction was added because none was needed.
- Verified: `tests/test_role_isolation.py::test_every_business_route_is_role_gated_as_expected` (a static audit over the real FastAPI dependency graph) extended and passing — confirms the 8 newly-opened routes use `require_owner_or_technician_context` and that `DELETE`/`events` still use `require_owner_context` only. 4 new tests in `tests/test_diagnostics_and_inspections_api.py` (one happy-path + one validation-guard test per module, using a real approved-estimate → work-order → technician-provisioning → assignment chain, not direct DB row inserts). Full suite green, ruff/pyright/`node --check` clean. Live-proven against a real Postgres: the store-layer scoping directly (bypassing the OpenAI-dependent estimate flow via the same orchestrator-stub technique the test suite itself uses), and separately over real HTTP with a real technician session cookie — confirmed correct list/read/create/update scoping and real `403`s on `DELETE`/`events` for that technician.

**Part F — Parts/Vendors: purchase-order + allocation workflow. SPLIT INTO TWO SLICES — Purchase Orders DONE (2026-07-15), Part Allocation NOT STARTED.** Sub-phase 3 shipped a vendor directory + parts inventory with a reorder-threshold flag, but explicitly not the `PurchaseOrder`/`PartAllocation` lifecycle originally planned. Given the scope (this is arguably two features), it was deliberately split into two reviewable PRs rather than one large one, matching this session's established practice for Parts D/E.

**Purchase Orders slice — DONE, awaiting merge.** `draft → submitted → partially_received → received / cancelled` lifecycle, owner-only (`app/purchase_order_store.py`, migration `020_purchase_orders`).
- Line items are set once at creation (no add/remove/edit-line-item endpoint) — matching the estimate-revision convention: if the order is wrong, cancel and recreate rather than editing in place. `unit_cost` is snapshotted onto the line item at order time, matching the existing `InvoiceLineItem` snapshot convention — a Part's own `unit_cost` changing later never retroactively changes an already-created order.
- Receiving (`receive_purchase_order_line_item`) takes `SELECT ... FOR UPDATE` row locks on both the line item and the Part row before checking/mutating quantities (same pattern as `payment_store.py::record_payment`'s existing overpayment race fix). Over-receipt is rejected outright, not silently clamped — duplicate-receipt protection. An append-only `purchase_order_receipts` table records every receiving event. PO status auto-transitions (`submitted` → `partially_received` → `received`) as line items get fully received.
- Decimal money throughout the store layer (`_money()`/quantize, matching `part_store.py`/`invoice_store.py`), converted to `float` only at the Pydantic-model boundary.
- Frontend: new "Purchase orders" nav item + list/detail/create-form view (`app/static/index.html`), matching the existing list+detail+form pattern; a draft-line-item builder mirrors the existing Inspections checklist-item UI pattern.
- **Not in scope for this slice**: technician access (owner-only for now, unlike Diagnostics/Inspections' Part E carve-out — purchasing/receiving stayed an owner-only action, a disclosed scope decision); dashboard Gross Profit/margin wiring (explicitly deferred per the original Part F description's own "once reliable" phrasing — the underlying cost data from this slice is real, but the dashboard integration itself is separate follow-up work); Part Allocation (see below).
- Verified: 9 new tests in `tests/test_purchase_orders_api.py` (totals/unit-cost-snapshot, unknown-vendor/part rejection, cross-owner isolation, status-transition guards, receiving updates inventory + auto-transitions status, over-receipt rejection, money-column-overflow rejection). Full suite green, ruff/pyright/`node --check` clean. Migration upgrade/downgrade round-tripped cleanly against real Postgres with a single linear head. Full lifecycle live-proven over real HTTP against real Postgres. `tests/test_role_isolation.py`'s static route-gate audit (real FastAPI dependency graph, not a hand-maintained list) confirms all 8 new routes are owner-only with no special-case entry needed.
- **A real concurrency bug was caught by independent review, not by the first live proof, and fixed before merge — full account below, not glossed over.** The first live-proof session (before independent review) ran two genuinely concurrent HTTP receive requests and observed one succeed and one get cleanly rejected — but that outcome was coincidental, not evidence of correctness. The `optimus-reviewer` agent independently re-derived the locking logic, judged it suspect (`SELECT ... FOR UPDATE` against just the line item's `.id` column does not refresh an already-loaded ORM object's other attributes in SQLAlchemy's identity map — a genuine gotcha, not the same pattern as `payment_store.py`'s existing fix despite the code comment claiming so), and reproduced a real bypass live against its own throwaway Postgres container: with the lock acquired but the object never refreshed, a concurrent request could read stale pre-lock quantities and pass the over-receipt check it should have failed. Fixed by re-querying both the purchase order and the line item with `.with_for_update().execution_options(populate_existing=True)` immediately after acquiring the lock, and by deriving the post-receipt auto-transition decision from the freshly-refreshed status rather than a pre-lock local variable (which had also been silently causing a second, related bug: two people legitimately receiving separate partial deliveries against the same PO at the same time could hit a confusing "cannot move from partially_received to partially_received" error). Both fixes were then independently re-verified with two new live-proof scripts against a fresh throwaway Postgres (not reused from the flawed first attempt): (1) the exact over-receipt race from the review reproduction now correctly yields one clean success and one clean rejection, not a confusing status error; (2) two legitimate non-conflicting concurrent partial receipts (4+4 against a 20-unit line item) now both succeed, proving the fix didn't overcorrect into blocking legitimate concurrent use. The review also caught and this fixes two smaller issues: a `po_number` generation race (COUNT-then-insert with no retry) now retries up to 3 times on a unique-constraint conflict before failing cleanly, and a missing upper bound on `unit_cost × quantity_ordered` that could overflow the `Numeric(10,2)` money columns and surface as a raw 500 now raises a clean 422 instead (covered by the new 9th test).

**Part Allocation slice — DONE, awaiting merge (2026-07-15).** Work-order part allocation with quantity-required/allocated/used/returned and inventory deduction/return (`app/part_allocation_store.py`, migration `021_part_allocations`).
- `create` records only `quantity_required` — no inventory movement yet. `allocate` pulls from `Part.quantity_on_hand`, rejected by default if insufficient; an explicit `override=True` + non-blank `override_reason` allows it anyway, clamping `Part.quantity_on_hand` to 0 (never negative — the pre-existing `quantity_on_hand >= 0` CHECK constraint from Sub-phase 3 is never touched or weakened) and recording the override + reason on the append-only `part_allocation_events` log. `use` marks allocated stock as consumed (bounded by `quantity_allocated - quantity_used`, no further inventory movement — it was already pulled at allocate time). `return` puts unused allocated stock back on the shelf (bounded by `quantity_allocated - quantity_used - quantity_returned`).
- Technician access reuses Phase 6 Part E's exact carve-out pattern: create/list/get/allocate/use/return are `OwnerOrTechnicianAuthContextDep`, scoped to the technician's own assigned work orders (`work_order_id.in_(select(WorkOrder.id).where(WorkOrder.assigned_technician_id == technician.id))`). The audit-event-history endpoint stays owner-only, matching every other module's precedent.
- Frontend: a "Parts" section embedded directly in the existing Work Order detail view (an "Add part requirement" form + a "Record parts action" form covering allocate/use/return, with an override checkbox+reason shown only for allocate) — not a separate standalone view, since allocations only make sense in the context of a specific work order.
- **Concurrency correctness was the explicit focus of this slice, given Purchase Orders' concurrency bug found in the immediately preceding PR.** Every locking function in `part_allocation_store.py` applies that exact fix pattern from the start (re-query with `.with_for_update().execution_options(populate_existing=True)` immediately after acquiring the lock, never trust a pre-lock cached attribute for a post-lock decision) rather than discovering it the same way again. Live-verified with a real concurrent-thread reproduction (two genuinely concurrent `allocate_part` calls against a Part with only 10 on hand, each requesting 6): exactly one succeeded, the other was cleanly rejected seeing the post-lock state, inventory never went negative, no double-deduction. **Independent review confirmed the fix applies correctly to `use_part_allocation` and `return_part_allocation` too** (not just `allocate_part`, which is the only one I live-tested) — read function-by-function and confirmed each re-selects with `populate_existing` before any bound check, with no gap where a pre-lock field is trusted post-lock. No Critical or High findings.
- **Independent review, disclosed Medium/Low items (no code changes needed — business-policy notes, not defects)**: (1) nothing caps `quantity_allocated` against `quantity_required` — an allocation can be `allocate`d repeatedly well past what was originally required, with no error and no override needed, since that's an "over-requirement" not an "insufficient stock" condition; left uncapped deliberately, since real shops legitimately sometimes need more than originally estimated (breakage, wrong first pull), matching this codebase's general precedent of not hard-capping quantity fields. (2) `part_allocations.work_order_id` cascades on work-order delete, same as every other work-order-child table in this codebase (notes, status events, invoices) — there is no work-order-delete endpoint today so this isn't exploitable, but a future hard-delete would silently discard allocation/event rows without crediting `Part.quantity_on_hand` back; worth remembering if a work-order-delete feature is ever added. (3) override-to-zero clamping loses the magnitude of the shortfall on `Part.quantity_on_hand` itself (only reconstructable by summing event deltas) — an accepted tradeoff given the pre-existing non-negative CHECK constraint is never weakened and the event log preserves full traceability.
- Verified: 10 tests in `tests/test_part_allocations_api.py` (unit-cost snapshot, allocate deducts inventory + rejects insufficient stock, override requires a reason + clamps to zero + records on the event log, use/return bounded correctly, cross-owner isolation, technician can create/allocate on own assigned work order, technician cannot access an unassigned work order's allocations, technician cannot access a *different* technician's own assigned work order's allocations either — added after independent review flagged this as a valuable untested-but-correct-by-code-reading gap, event history tracks all three action types). `tests/test_role_isolation.py`'s static route-gate audit extended and passing. Full suite green, ruff/pyright/`node --check` clean. Migration upgrade/downgrade round-tripped cleanly against real Postgres with a single linear head. Full lifecycle live-proven over real HTTP against real Postgres.
- This closes Phase 6 Part F entirely — both slices (Purchase Orders and Part Allocation) are now done.

Supplier cost and markup must never reach customer-facing estimates/invoices/approval pages/PDFs — only the final customer parts price; this remains true after this slice since nothing here touches any customer-facing surface yet (POs and receiving are purely internal/owner-facing).

**Part G — Reports completion. SPLIT INTO SLICES, following Part F's practice — Slice 1 (Payment Activity + Technician Time/Labor Cost) DONE, merged via PR #38 (2026-07-16). Slice 2a (Gross Profit/Margin + Inventory Valuation) DONE, merged via PR #39 (2026-07-16). Slice 3 (Parts Usage + Vendor Purchasing) DONE, merged via PR #40 (2026-07-16). Slice 4 (Work Order Cycle Time + Comebacks) DONE, merged via PR #41 (2026-07-16). Slice 5 (Diagnostic Findings + Inspections) DONE, awaiting merge (2026-07-17).** Sub-phase 7 shipped a read-only view reusing the existing dashboard/invoice aggregation endpoints (no `SavedReportRequest` table, no new backend aggregation) — explicitly missing payment-activity, technician-time, commission, gross-profit, inventory-valuation, parts-usage, vendor-purchasing, cycle-time/comeback, and diagnostic/inspection reports (documented honestly in the view itself). Each slice deliberately covers only the reports buildable from existing schema without new instrumentation; the remainder is scoped out below as a tracked follow-up rather than folded into one oversized PR.

- New `app/report_store.py`: `get_payment_activity_report(*, db, auth, date_from, date_to)` — cross-invoice payment query joined to `Invoice` for `invoice_number`, correctly nets reversal rows (stored with a negative `amount` per the `ck_invoice_payments_amount_sign` CHECK constraint — verified this invariant is doubly enforced, both by `payment_store.py::void_payment` always negating and by the DB constraint independently, so the report's raw-sum netting can't be fooled by a non-negative reversal) and builds `by_method`/`by_applies_to` breakdown groups. `get_technician_time_report(*, db, auth, date_from, date_to)` — per-technician clocked hours from `TechnicianTimeEntry` (non-archived technicians only), excludes still-open entries from hour totals but tracks `open_entry_count`, computes `labor_cost = hourly_cost × hours` only when `hourly_cost` is set (tracks `technicians_missing_hourly_cost` otherwise), and returns `billed_hours`/`commission` as honest `DashboardMetric(available=False, unavailable_reason=...)` disclosures reusing the existing dashboard pattern rather than fabricating numbers OptimusOS can't actually compute yet (no work-order link on time entries; no per-technician commission rate, only hourly cost).
- Two new owner-only routes, `GET /api/reports/payment-activity` and `GET /api/reports/technician-time`, following the exact `get_dashboard_summary_record` template (30-day default window, `date_from >= date_to` → 422, `SQLAlchemyError` → 503). No change needed to `tests/test_role_isolation.py`'s static route-gate audit — it inspects the live FastAPI dependency graph and defaults any unlisted route to owner-only, so both new routes were correctly gated with zero manual list maintenance.
- Frontend: `loadReports()` extended to fetch both new endpoints alongside the existing dashboard/invoice calls, rendering two new report cards. The "not yet available" disclosure text was narrowed from "payment activity, technician time, and commission" to just "technician commission, and billed-vs-clocked hours" to stay accurate now that two of the three are shipped.
- **Independent review finding, fixed before merge**: the payment-activity table originally rendered the `by_applies_to` and `by_method` breakdowns as one flat list with no separator — since both are independent categorizations of the *same* payments (not additive line items), an owner skimming the table could misread it as summable and roughly double their mental total. Fixed with a `report-table-subhead` sub-header row ("By type" / "By payment method") separating the two groups.
- **Independent review finding, accepted and documented rather than fixed**: `get_technician_time_report` filters entries on `clock_in_at` falling inside the window, not on clock-in/clock-out *overlap* with the window — a shift that started before `date_from` and ended inside the window has none of its in-window hours counted. Accepted since boundary-spanning shifts are rare in this shop's usage; documented in a code comment, `docs/context/KNOWN_ISSUES.md`, and pinned by a new regression test (`test_technician_time_report_shift_spanning_window_start_is_excluded`) so the behavior can't silently change unnoticed.
- **Independent review finding, fixed as a hardening item (not an active bug)**: `total_labor_cost` was accumulated via an unnecessary `Decimal → float → Decimal` round-trip per technician; changed to keep the running total in `Decimal` from the un-rounded product, converting to `float` only once at the response boundary.
- Independent review confirmed correct and unchanged: cross-owner isolation on both reports (verified transitively safe even where a query isn't directly `owner_user_id`-filtered, via the FK/ownership chain back to an owner-scoped parent row), Decimal/float boundary handling elsewhere in the payment report, the `PaymentAppliesTo` frontend label mapping, all four new DOM ids existing exactly once, and no SQLAlchemy identity-map staleness risk (both queries are standalone reads, not composed with an earlier write in the same request).
- Verified: 11 tests in `tests/test_reports_api.py` (payment totals/breakdown/reversal-netting, date-range filtering, invalid-range 422, cross-user isolation; technician hours/labor-cost computation, missing-hourly-cost flagging, archived-technician exclusion, date-range filtering including the window-boundary case above, invalid-range 422, cross-user isolation). Full suite green (343 passed, 2 pre-existing unrelated Redis-skips), ruff/pyright/`node --check` clean. Live-proven against a real, freshly-migrated throwaway Postgres 16 container (not SQLite): a real invoice with two real payments (one voided) and real `TechnicianTimeEntry` rows confirmed correct reversal netting, breakdown math, labor-cost computation, and cross-owner isolation against real SQL — container torn down after.
- **Deferred to a tracked follow-up as of Slice 1, later addressed in Slice 2a/3/4/5**: gross-profit/margin, inventory valuation, parts usage, vendor purchasing, cycle-time, comeback rate, and diagnostic/inspection findings (all DONE below — comeback rate unblocked by reporting on the existing manual `is_comeback` flag rather than attempting undefined auto-detection). **Still deferred, not started**: CSV export. Report scheduling/delivery stays a separate future phase regardless.

**Part G Slice 2a — Gross Profit/Margin + Inventory Valuation. DONE, merged via PR #39 (2026-07-16).** Picked as the next slice after Slice 1 merged: Gross Profit was the highest-value item left (previously permanently hardcoded `available=False` on the dashboard) and only became buildable once Part F's Part Allocation work gave the codebase a real per-unit cost record (`PartAllocation.unit_cost_snapshot`); Inventory Valuation was bundled in as a second slice component since it reuses the same `Part` catalog data and is a comparably small, self-contained addition.

- `app/dashboard_store.py`: new `_period_cogs()` helper sums `PartAllocation.unit_cost_snapshot × quantity` for `PartAllocationEvent` rows with `event_type='used'` in the requested window (owner-scoped via `PartAllocation.owner_user_id`), using a `case()`-based conditional aggregate rather than `func.sum(...).filter(...)` specifically because `.filter()` on an aggregate emits Postgres-only `FILTER (WHERE ...)` SQL that the fast SQLite-backed test suite can't run — a dialect-compatibility choice made during design, not a fix for a failure. Quantities used with no recorded `unit_cost_snapshot` are excluded from the COGS dollar sum entirely (not assigned a fabricated cost) and counted separately (`missing_cost_quantity`), surfaced via a new LOW-priority `DashboardInsight` (`key="parts-missing-cost-data"`, `link_view="parts"`, wired to `openDashboardInsightTarget` in `app.js`) when nonzero. `gross_profit` metric now genuinely computed (`revenue − COGS`); `gross_profit_margin` is `available` only when `current.revenue > 0`, with an honest `unavailable_reason` otherwise. **This is a "usage-period" approximation, not exact accrual accounting** — see the new `docs/context/KNOWN_ISSUES.md` entry for the full explanation of why (no FK links invoice line items back to the `Part` catalog, so cost and revenue can land in different reporting windows near a boundary).
- `app/static/index.html`: the `gross_profit_margin` gauge card's markup was upgraded from a permanently-unavailable placeholder to the full renderable gauge structure (matching the existing `accounts_receivable_health` pattern) — no new JS logic needed, since the existing generic `renderDashboardMetrics`/`renderGauge` machinery already handles both available/unavailable states as long as the DOM has the right `data-role` elements.
- New `app/report_store.py::get_inventory_valuation_report(*, db, auth)` — a point-in-time snapshot (not date-ranged, unlike every other report in this file) over non-archived `Part` rows for the current owner: `total_valuation` sums `quantity_on_hand × unit_cost` only for parts with `unit_cost` set (parts missing one are excluded from the dollar total, not assigned a fabricated cost, and counted separately in `parts_missing_cost_count` — the same disclose-the-gap pattern used throughout this codebase's reporting); `low_stock_parts` lists parts where `reorder_threshold IS NOT NULL AND quantity_on_hand <= reorder_threshold`, each carrying a `vendor_display_name` via an outer join to `Vendor` (so a part with no vendor assigned still appears, just without a vendor name).
- New owner-only route `GET /api/reports/inventory-valuation` (no date params — a snapshot, not a range), and two new Pydantic models (`LowStockPartRead`, `InventoryValuationReportResponse`) in `app/models.py`. No change needed to `tests/test_role_isolation.py`'s static route-gate audit — the new route was correctly gated with zero manual list maintenance, same as every prior slice's new routes.
- Frontend: `loadReports()` extended to also fetch `/api/reports/inventory-valuation` (no date range) alongside the existing calls; new "Inventory valuation" report card with a nested low-stock sub-table (part, on-hand/threshold, vendor).
- Verified: 4 new tests in `tests/test_dashboard_api.py` (gross profit subtracts real parts cost, negative gross-profit-margin isn't clamped by the backend, flags missing-cost-data via the insight, cross-user isolation) plus a fixed pre-existing test whose old `gross_profit.available is False` assertion was stale now that the metric is genuinely computed. 5 new tests in `tests/test_reports_api.py` (sums costed parts, flags missing unit cost — confirmed a zero-quantity uncosted part correctly does *not* count toward the missing-cost disclosure, since there's no stock gap to disclose — lists low-stock parts with vendor name, excludes archived parts, cross-user isolation). Full suite green (352 passed, 2 pre-existing unrelated Redis-skips), ruff/pyright/`node --check` clean. Live-proven against a real, freshly-migrated throwaway Postgres 16 container: real invoice + real part usage (one costed, one uncosted) confirmed gross profit computed to the exact expected dollar figure with the missing-cost insight correctly disclosing the excluded quantity; real parts (costed, uncosted, low-stock, archived) confirmed the inventory valuation total, missing-cost count, low-stock detection with correct vendor name, and archived-part exclusion all matched exact expected values; a second owner confirmed both features correctly return zero/empty. Container torn down after.
- **Independent review (`optimus-reviewer`) findings, all fixed before merge**: (1) `docs/context/CURRENT_STATE.md`'s Overview Dashboard section still claimed Gross Profit/Gross Profit Margin "are never fabricated... render an honest 'not available' state" — stale now that they're genuinely computed; corrected to describe the real computation and point at this slice's `KNOWN_ISSUES.md` entry. (2) The `_period_cogs` docstring referenced a `KNOWN_ISSUES.md` disclosure that didn't exist yet at review time (it was added later in the same session, closing the gap). (3) `renderGauge` in `app/static/app.js` clamped its displayed label to `[0, 100]`, which is correct for `approval_conversion_rate`/`accounts_receivable_health` (mathematically bounded) but wrong for `gross_profit_margin`, which can legitimately go negative given the usage-period COGS approximation — a loss period would have silently displayed "0%" instead of the real negative figure. Fixed by keeping the ring's visual clamp (it can't render past its own bounds) but reading the label text from the raw, unclamped `metric.value`; pinned by a new backend regression test (`test_dashboard_gross_profit_margin_can_go_negative`) confirming the API itself never clamps the value it returns.
- **Independent review, nice-to-have items noted, not fixed (non-blocking)**: no code comment explaining `low_stock_parts`' sort-by-most-below-threshold ordering (self-explanatory from the code, low value to spell out); `_period_cogs` runs as its own aggregate query in addition to the pre-existing `_period_aggregate` calls (not a real perf concern at this shop's scale, single efficient query, not N+1); no dedicated composite index on `PartAllocationEvent` for the owner/event-type/date filter path (fine at single-shop scale, revisit if this table grows large); a part with `quantity_on_hand == 0` and no `unit_cost` is deliberately excluded from `parts_missing_cost_count` (explicitly tested, consistent with the "nothing to disclose when there's no stock" design) — flagged as worth confirming with the owner is the desired scope, not treated as a defect.
- Independent review confirmed correct with no changes needed: `_period_cogs`'s join/aggregation logic against `PartAllocationEvent`'s append-only `'used'` events (no double-counting across allocate→use→return→use-again sequences); cross-owner isolation on both new queries; Decimal/float boundary handling in `get_inventory_valuation_report` (matches the existing `get_payment_activity_report` pattern) and in `_period_cogs` (matches the existing `_period_aggregate` pattern); the `_use_part` test helper's create→cost-update→allocate→use sequencing against `Part.model_fields_set` semantics; the new route's dependency/error-handling pattern matching its sibling report routes; frontend gauge/table wiring for all available/unavailable/empty states.

**Part G Slice 3 — Parts Usage + Vendor Purchasing. DONE, merged via PR #40 (2026-07-16).** Picked as the next slice after Slice 2a merged: both reports reuse data sources already wired up by prior slices (`PartAllocationEvent`/`PartAllocation.unit_cost_snapshot` for Parts Usage, same source as Gross Profit's COGS; `PurchaseOrder`/`Vendor` for Vendor Purchasing, from Part F) and follow the exact same date-ranged report pattern as every other Part G slice, keeping this a small, reviewable, self-contained addition.

- New `app/report_store.py::get_parts_usage_report(*, db, auth, date_from, date_to)` — a per-part breakdown of the same "used" `PartAllocationEvent` activity that feeds the dashboard's Gross Profit COGS figure, using the identical `case()`-based conditional-aggregation query shape as `_period_cogs` (join `PartAllocationEvent → PartAllocation → Part`, group by part). `cost_total` per part only sums usage with a recorded `unit_cost_snapshot`; usage missing one is excluded from the dollar total and counted separately in `quantity_missing_cost` (same disclose-the-gap pattern used throughout this codebase's reports). Parts sorted most-used-first. **Does not tie out to the penny with `_period_cogs`** at a window boundary — this file uses the exclusive `date_to <` convention shared by every other report here, while `_period_cogs` uses inclusive `<=`; documented in both docstrings and in `docs/context/KNOWN_ISSUES.md`.
- New `app/report_store.py::get_vendor_purchasing_report(*, db, auth, date_from, date_to)` — a per-vendor breakdown of purchase-order spend. Only POs with `submitted_at` in the window count (a `draft` PO never became a real commitment); submitted-then-cancelled POs are excluded from spend and counted separately in `cancelled_order_count`. **Known, disclosed imprecision**: a PO cancelled from `partially_received` status has some real, already-received spend, but since `PurchaseOrder.total` is fixed at creation time and isn't reduced by partial receiving, the report excludes the order's *entire* total rather than fabricate a partial figure — surfaced via `cancelled_order_count`, not hidden. A fully `received` order can never be cancelled afterward (terminal status), so this only bites the partial-receive-then-cancel path specifically. Documented in the function's docstring and `docs/context/KNOWN_ISSUES.md`, pinned by a dedicated regression test.
- Two new owner-only routes, `GET /api/reports/parts-usage` and `GET /api/reports/vendor-purchasing`, following the exact same 30-day-default-window/422/503 template as every other report route in this file. No change needed to `tests/test_role_isolation.py`'s static route-gate audit — both new routes correctly defaulted to owner-only with zero manual list maintenance.
- New Pydantic models in `app/models.py`: `PartUsageEntryRead`/`PartsUsageReportResponse`, `VendorPurchasingBreakdownItem`/`VendorPurchasingReportResponse`.
- Frontend: `loadReports()` extended to fetch both new endpoints (same 30-day default range as the other date-ranged reports); two new report cards ("Parts usage", "Vendor purchasing") with disclosure notes for missing-cost usage and excluded cancelled orders.
- **Independent review (`optimus-reviewer`) findings, both fixed before merge**: (1) the `get_parts_usage_report` docstring originally overclaimed exact equivalence with `_period_cogs`'s totals — softened to explain the `<`/`<=` `date_to` boundary difference between the two functions. (2) `get_vendor_purchasing_report`'s docstring originally asserted cancelled orders always mean "nothing was actually purchased," which isn't true for the partially-received-then-cancelled case — corrected with the imprecision explained above, plus a new regression test (`test_vendor_purchasing_report_partially_received_then_cancelled_excludes_full_total`) pinning the current (imperfect) behavior, and a companion test (`test_vendor_purchasing_report_counts_received_orders_as_spend`) confirming a normal received order is still correctly counted.
- **Independent review, nice-to-have item, fixed as a hardening item (not an active bug)**: `get_parts_usage_report`'s totals were originally re-derived via `sum()` over already-float-converted per-part values in Python; changed to accumulate in `Decimal` from the raw SQL row values through to a single final `float()` cast, matching `get_vendor_purchasing_report`'s style in the same diff and the rest of this file's established Decimal-until-last-cast convention.
- Independent review confirmed correct with no changes needed: join/aggregation shape and no double-counting risk in `get_parts_usage_report` (straight many-to-one FK chain, `GROUP BY` correctly rolls up multiple allocations per part); `unit_cost_snapshot IS NULL` handling identical to `_period_cogs`'s existing idiom; `quantity_delta` sign convention (always positive for `'used'` events, confirmed via `part_allocation_store.py`); owner-scoping on both new queries; draft-exclusion logic (`submitted_at IS NOT NULL`); Decimal/float handling in `get_vendor_purchasing_report`'s `by_vendor` accumulation; route/model wiring consistency with sibling report routes; frontend empty-state and disclosure-note rendering for both new cards.
- Verified: 5 new tests in `tests/test_reports_api.py` for Parts Usage (sums costed usage, flags missing cost and confirms sort-by-quantity-used-first regardless of cost availability, date-range exclusion, invalid-range 422, cross-user isolation) and 7 new tests for Vendor Purchasing (sums submitted orders by vendor, excludes drafts and discloses cancelled orders, counts received orders as spend, the partially-received-then-cancelled edge case above, date-range exclusion, invalid-range 422, cross-user isolation). Full suite green (364 passed, 2 pre-existing unrelated Redis-skips), ruff/pyright/`node --check` clean. Live-proven against a real, freshly-migrated throwaway Postgres 16 container: real part usage (one costed, one uncosted, different quantities) confirmed the parts-usage report's totals, missing-cost disclosure, and most-used-first sort order (including the uncosted-but-higher-quantity part correctly sorting first) all matched exact expected values; real purchase orders across two vendors (submitted, submitted-then-cancelled, and an unsubmitted draft) confirmed the vendor-purchasing report's total spend, per-vendor breakdown, highest-spend-first ordering, cancelled-order exclusion/disclosure, and draft exclusion all matched exact expected values; a second owner confirmed both reports correctly return empty/zero. Container torn down after.

**Part G Slice 4 — Work Order Cycle Time + Comebacks. DONE, merged via PR #41 (2026-07-16).** Picked as the next slice after Slice 3 merged: both metrics reuse `WorkOrder`/`WorkOrderStatusEvent` data that already existed (no new schema), and combining them into one report made sense since they share the identical "completed work orders in the window" query base rather than running two near-identical queries separately. This also **unblocks the previously-blocked Comeback Rate item**: rather than inventing an automatic same-vehicle/same-complaint detection rule the owner hasn't defined, the report surfaces the rate from the *existing* manual `WorkOrder.is_comeback` checkbox (added back in Phase 5.6 sub-phase 2, previously unused by any report) — an honest signal that was already there, not a new inference.

- New `app/report_store.py::get_work_order_cycle_time_report(*, db, auth, date_from, date_to)` — joins `WorkOrder` to its `WorkOrderStatusEvent` row where `to_status='completed'`, filtered to events in the requested window. Cycle time = elapsed time from `WorkOrder.created_at` to that completion event's `created_at` — **total elapsed calendar time, not active wrench time**, so a job that spent days waiting on a backordered part still counts that wait; documented in the response model's docstring and the frontend disclosure note. Since `completed` is a terminal status in `work_order_store.py`'s `TRANSITIONS` dict (empty tuple, no transition leads out of it — independently re-verified by the reviewer against the actual dict, not just the docstring's claim), each work order can have at most one `to_status='completed'` event, so the join can't fan out or double-count. Reports average/median/fastest/slowest cycle time in hours. `comeback_rate_percent` = (completed work orders in the window with `is_comeback=True`) ÷ (total completed work orders in the window) × 100 — explicitly disclosed everywhere it's surfaced as reflecting the owner's own manual flag, not automatic detection.
- New owner-only route `GET /api/reports/work-order-cycle-time`, following the exact same 30-day-default-window/422/503 template as every other report route in this file. No change needed to `tests/test_role_isolation.py`'s static route-gate audit.
- New Pydantic model `WorkOrderCycleTimeReportResponse` in `app/models.py`.
- Frontend: `loadReports()` extended to fetch the new endpoint; one new report card ("Work order cycle time & comebacks") rendered as a small stat table (average/median/fastest/slowest cycle time, comeback rate with the raw count), with a disclosure note covering both the elapsed-calendar-time definition and the owner-flagged nature of the comeback figure.
- **Independent review finding, fixed before merge**: the comeback-rate disclosure existed (both in the backend docstring and inline in the stat row as "owner-flagged") but wasn't in the shared top-level note paragraph, and the median calculation's even-count branch (`(a+b)/2` averaging of the two middle values) had no dedicated test — the only cycle-time test used an odd count (3 work orders), so the even-count arithmetic was correct-by-inspection but unverified. Fixed both: the report card's note paragraph now explicitly states "Comeback rate reflects only work orders the owner manually flagged... OptimusOS does not automatically detect repeat repairs," and the stat row label itself now reads "Comeback rate (owner-flagged)"; added `test_work_order_cycle_time_report_computes_median_for_even_count` (4 work orders at asymmetric 1h/2h/3h/100h, median 2.5 vs. average 26.5 — chosen so the two values can't be confused and the test can't accidentally pass under the wrong formula).
- Independent review confirmed correct with no changes needed (verified by direct inspection of `work_order_store.py`'s `TRANSITIONS` dict and `_append_status_event`, not just trusted from the docstring): the terminal-status/no-fan-out claim; cross-user isolation (filtered only on `WorkOrderStatusEvent.owner_user_id`, safe because that column can never diverge from its parent `WorkOrder`'s owner since `_append_status_event` always stamps it from the same `effective_owner_id(auth)` call); the odd-count median branch's arithmetic; the `_set_cycle_time` test helper's direct-timestamp-manipulation approach (mirrors the existing `_add_time_entry` pattern from the Technician Time report's tests, doesn't interact with any CHECK constraint or trigger); route/response wiring consistency with sibling report routes; all loading/populated/empty/error frontend states.
- Verified: 7 tests in `tests/test_reports_api.py` (average/median/fastest/slowest across 3 work orders with engineered 10h/20h/30h cycle times, the new even-count median case above, comeback rate from the manual flag, empty-state returns all zeros, date-range exclusion, invalid-range 422, cross-user isolation). Full suite green (371 passed, 2 pre-existing unrelated Redis-skips), ruff/pyright/`node --check` clean. Live-proven against a real, freshly-migrated throwaway Postgres 16 container: 3 real completed work orders with engineered 8h/24h/40h cycle times (one flagged `is_comeback=True`) confirmed the report's average (24.0h), median (24.0h), fastest (8.0h), slowest (40.0h), and comeback rate (33.3%, 1 of 3) all matched exact expected values; a second owner confirmed a fully-zeroed report. Container torn down after.

**Part G Slice 5 — Diagnostic Findings + Inspections. DONE, awaiting merge (2026-07-17).** Picked as the next (and likely final Part G reporting) slice after Slice 4 merged: both existing logs (`DiagnosticFinding`, `Inspection`) had zero reporting coverage despite being real, already-captured technician-observation data — combined into one report since they're both read-only, both date-ranged on `created_at`, and neither alone was substantial enough to justify its own slice.

- New `app/report_store.py::get_diagnostic_inspection_report(*, db, auth, date_from, date_to)` — counts `DiagnosticFinding` rows and `Inspection` rows created in the window. Diagnostic findings have no structured status/severity/category column anywhere in the schema (only free-text `codes`/`symptoms`/`tests_performed`/`conclusion`), so `findings_missing_conclusion` (findings with no `conclusion` recorded, i.e. diagnosis still open) is the honest disclosure available rather than a fabricated category. Inspection `items` is stored as an untyped JSON column at the DB level, but the application layer strictly enforces its shape on every write via `InspectionItem` (`label`, `status: Literal["ok","attention","fail"]`, `note`) — so `items_ok`/`items_attention`/`items_fail` is a genuine structured breakdown, read by revalidating each raw JSON item through `InspectionItem.model_validate(item)` (matching the same pattern `inspection_store.py` already uses) rather than trusting a raw dict key, so a hypothetical malformed/corrupted status value raises loudly instead of being silently miscounted as "ok". **Deliberate design choice**: activity is counted by `created_at` falling in the window regardless of current `is_archived` status — unlike the earlier point-in-time Inventory Valuation report (which excludes archived parts entirely), this is an activity-in-period report like Payment Activity/Parts Usage/Vendor Purchasing/Cycle Time, so archiving something afterward shouldn't retroactively erase that the observation was made during the period. Documented in the response model's docstring, the function's docstring, and the frontend note text.
- New owner-only route `GET /api/reports/diagnostic-inspection`, following the exact same 30-day-default-window/422/503 template as every other report route in this file. No change needed to `tests/test_role_isolation.py`'s static route-gate audit.
- New Pydantic model `DiagnosticInspectionReportResponse` in `app/models.py`.
- Frontend: `loadReports()` extended to fetch the new endpoint; one new report card ("Diagnostics & inspections") rendered as a small stat table.
- **Independent review findings, both fixed before merge**: (1) the frontend note text originally omitted the "counts activity regardless of later archiving" disclosure that every sibling note in the same function includes for its own non-obvious methodology choice (Inventory Valuation's "not date-ranged," Cycle Time's "not active wrench time" / "owner-flagged") — fixed by appending "including any since archived" to the note. (2) the inspection-item status read originally used raw `item.get("status")` with anything not exactly `"attention"`/`"fail"` silently bucketed as `"ok"` — while the write path is fully guarded today (no route exists that writes `Inspection.items` without going through `InspectionCreate`/`InspectionUpdate`'s Pydantic validation), this diverged from the codebase's own established pattern for reading this exact field (`inspection_store.py` revalidates via `InspectionItem.model_validate(item)` before reading `.status`) and would have silently laundered a hypothetical future data-quality problem (e.g. a corrupted `fail` record) into the safest-looking bucket instead of surfacing it. Fixed to match `inspection_store.py`'s pattern.
- **Independent review nice-to-have, fixed as a hardening item**: no test covered an inspection created with an empty `items` list (a realistic shape — a technician starting an inspection before filling in checklist items). Added `test_diagnostic_inspection_report_counts_inspection_with_no_items_yet`, confirming it correctly increments `inspection_count` while contributing zero to every item-count field.
- Independent review confirmed correct with no changes needed: `findings_missing_conclusion`'s SQL/Python boundary (fetched via SQL, filtered in Python with `not conclusion`, verified consistent with `DiagnosticFindingBase`'s validator which already converts an empty/whitespace-only string to `None` before persistence, so there's no live empty-string-vs-NULL ambiguity to handle); the "count regardless of archived" choice is applied consistently on both queries with no accidental `is_archived` filter leaking in from another report's query shape; cross-user isolation on both new queries; the frontend's empty-state condition correctly handles the report's two independent counts (renders the stat table if either is nonzero, matching that every derived field is necessarily zero when its parent count is zero); route/response wiring consistency with sibling report routes.
- Verified: 8 tests in `tests/test_reports_api.py` (counts findings and missing-conclusion, breaks down inspection item status across a multi-item and a single-item inspection, the new empty-items-list case above, confirms archived activity created in-window is still counted, empty-state returns all zeros, date-range exclusion, invalid-range 422, cross-user isolation). Full suite green (379 passed, 2 pre-existing unrelated Redis-skips), ruff/pyright/`node --check` clean. Live-proven against a real, freshly-migrated throwaway Postgres 16 container: 2 real diagnostic findings (one with a conclusion then archived, one still open) correctly counted as 2 findings / 1 missing-conclusion (with the archived one still included); 2 real inspections (a 3-item inspection with one of each status, plus a 1-item inspection) correctly counted as 2 inspections / 4 items / 2 ok / 1 attention / 1 fail; a second owner confirmed a fully-zeroed report. Container torn down after.

**This closes Phase 6 Part G's reporting-content scope** — every report identified as buildable from existing schema is now shipped. Only CSV export (a cross-cutting UI feature, not a new report) and report scheduling/delivery (explicitly deferred to a separate future phase from the start) remain.

**Part G CSV export. DONE, awaiting merge (2026-07-17).** Final Part G item, picked up after Slice 5 merged: a generic, reusable client-side CSV export for the Reports view — pure frontend, no backend changes (no new routes, no new report_store.py functions), since every report already renders its data into a real DOM table and the export need is "let the owner get this into a spreadsheet," not a separate server-generated export format.

- New `app/static/app.js` functions: `csvEscapeCell` (RFC 4180 quoting — quotes a cell if it contains a comma/quote/CR/LF, doubling internal quotes; also prefixes a cell starting with `=`/`+`/`-`/`@` with a leading `'` as defense-in-depth against CSV/formula injection in Excel/Sheets, since RFC 4180 quoting alone doesn't prevent that), `reportTableToCsv` (reads a `<table>`'s `<thead>` row if present plus every `<tbody>` row via `textContent.trim()`, joins with `\r\n`), `exportReportTableAsCsv` (resolves a table body element id to its parent `<table>`, converts to CSV, triggers a client-side download via `Blob` + a temporary `<a download>` + `URL.createObjectURL`/`revokeObjectURL` — no server round-trip, no new endpoint). `initializeReports()` gained one event-delegation listener on `#reports-content` catching clicks on any `[data-export-csv]` button. A new `reportsExportReady` flag (set `false` at the start of every `loadReports()` call, `true` only once every table has finished rendering successfully) guards the export buttons against exporting stale "Loading…"/"Failed to load" placeholder text as if it were real data.
- `app/static/index.html`: every report card in the Reports view gained a small "Export CSV" button next to its heading, each carrying `data-export-csv="<real-tbody-id>"` and `data-export-filename="<kebab-case-name>"`. The Inventory Valuation card (two tables — summary + "Low stock" list) and, after a review finding, the Payment Activity card (now split into two real tables — "By type" and "By payment method" — each with its own button) both got two separate export buttons.
- `app/static/styles.css`: new `.report-card-heading` rule (flex, space-between) so a heading/subhead and its export button sit on one line without disturbing existing card spacing.
- New `tests/e2e/test_reports_csv_export.py` — two real-browser Playwright tests (this repo's existing `live_server`/`synthetic_owner` fixtures: real Postgres, real uvicorn, real login). The first clicks the Work Order Status Summary card's export button (guaranteed non-empty even for a brand-new owner, and its last row's label contains a literal comma, so it also exercises real CSV-quoting) and asserts the downloaded file's *exact* lines, not just that expected text appears somewhere in it. The second seeds a real technician + a real closed time entry directly against the live server's own database (mirroring `test_core_workflow.py`'s existing seeding pattern) to exercise the `<thead>`-detection branch of `reportTableToCsv`, which the first test's headerless table never touches, and asserts the exported header row and data row are exactly correct. No backend Python changed, so no new unit tests were needed; the e2e tests are the only new coverage, since the CSV logic itself only exists in the browser.
- **Independent review (`optimus-reviewer`) findings, all fixed before merge**: (1) Payment Activity's combined single-table-with-subhead-rows rendering meant its CSV export interleaved a 1-column section-header row between 2-column data rows — a real financial-reporting risk, since a user summing the exported "amount" column in a spreadsheet would double-count collected revenue across the two independent by-type/by-method breakdowns of the *same* payments. Fixed by splitting Payment Activity into two real, separately-exportable tables (mirroring Inventory Valuation's existing two-table pattern), removing the ragged subhead-row-in-tbody structure entirely. (2) Invoice Status Summary's "Showing N of M invoices" pagination caveat was appended as an extra 1-column row inside the same tbody as the 2-column status/count rows, producing the same ragged-CSV problem, and silently under-reporting invoice totals beyond the first 100 fetched without a structural caveat. Fixed by moving that text out of the table entirely into a proper `<p class="report-card-note">`, matching the pattern every other report card already uses for its own caveats. (3) The export buttons had no guard against clicking mid-load or after a fetch failure, so a user could download a "CSV" containing only the literal text "Loading…" or "Failed to load: ...", with a filename giving no indication it wasn't real data. Fixed with the `reportsExportReady` flag described above. (4) The original e2e test only covered a headerless stat table with weak substring assertions that would still pass under several classes of real bugs (swapped columns, dropped/reordered rows). Fixed by strengthening to exact-line assertions and adding a second test that seeds real data into a `<thead>`-bearing table to close the coverage gap on that code path.
- **Independent review nice-to-have, fixed as a hardening item**: `csvEscapeCell` didn't defend against CSV/formula injection (a cell starting with `=`/`+`/`-`/`@` can be interpreted as a formula by Excel/Sheets regardless of RFC 4180 quoting). Given the current single-owner trust model this was low-severity (the only plausible sources — vendor names, part descriptions — are owner-authored, so an owner could only inject into their own export), but cheap to close given this app's stated trajectory toward broader staff/role accounts. Fixed by prefixing such cells with a leading `'`.
- Independent review confirmed correct with no changes needed: the core RFC 4180 escaping logic (traced by hand through lone-comma, leading-quote, trailing-quote, mid-string-quote, and LF-vs-CRLF cases, all correct); DOM traversal robustness (all 12 `data-export-csv` values verified to resolve to a real, uniquely-occurring `<tbody id>` nested in a `<table>`, including the two split Inventory Valuation and (now) Payment Activity table pairs); the client-side-only design's safety boundary (no new endpoint, bounded strictly to whatever the current user's own session has already rendered, inherits the existing owner-only gating on `#view-reports` and its underlying `/api/reports/*`/`/api/dashboard/summary` endpoints for free); no CSP concern with the `Blob`/`URL.createObjectURL`/`<a download>` pattern against this app's existing CSP.
- Verified: `ruff format`/`ruff check`/`pyright` clean (no Python touched by this diff). Full non-e2e suite unchanged at 379 passed, 2 pre-existing unrelated Redis-skips. `node --check app/static/app.js` OK. `tests/test_official_ui.py`'s CSP checks (`test_index_html_has_no_inline_scripts`, `test_index_html_has_no_inline_style_attributes`) pass. Both e2e tests (the strengthened original plus the new `<thead>`-coverage test) passed in a real browser against a real throwaway Postgres 16 container + real uvicorn server, run together with the pre-existing full-workflow e2e test (3 passed total), session-scoped Postgres container torn down cleanly.

**This closes Phase 6 Part G entirely** — every report and the CSV export capability are now shipped. Only report scheduling/delivery remains, and that was explicitly scoped as a separate future phase from the very first Part G slice, not part of Part G's own completion bar.

**Part H — Security/production hardening. IN PROGRESS — approval-token revocation, multi-instance rate limiting, and a structured-logging foundation DONE (2026-07-14), merged.** Threat model (approval links, session cookies, owner/technician auth, public endpoints, PDF generation, Square integration, file/document exposure, cross-tenant access); ~~approval-token revocation (graduates from deferred to required)~~; ~~rate limiting re-verified for multi-instance reality (current limiter is in-process memory, single-instance only)~~; structured security event logging (foundation done, event taxonomy not yet); OpenAI usage/cost logging before real money flows regularly; customer-data retention policy, export process, and deletion process (with required financial-record retention exceptions); backup verification + restore rehearsal + deployment rollback re-proven on production infrastructure; monitoring requirements for health/readiness/errors/disk-space/database. Don't claim monitoring is active unless actually configured and verified.

**Structured-logging foundation, sub-item detail (2026-07-14):** the only prior logging setup was `logging.basicConfig(level=...)` — plain text, no request correlation, nothing machine-parseable for log aggregation. New `app/observability.py`: `JsonLogFormatter` (structured JSON per line: timestamp/level/logger/message/request_id plus any `extra` fields — deliberately does not include the raw request body or headers, so it can't leak passwords/tokens by construction, not just by discipline), `RequestIdLogFilter` (attaches the current request's id, via a `contextvars.ContextVar`, to *any* log record emitted anywhere during that request's handling — not just the middleware's own lines, so log lines from deep inside store/service code get the same correlation id for free), and `install_request_context_middleware` (assigns a UUID per request, sets `X-Request-ID` on the response, logs one structured `"request completed"` line per request with method/path/status/duration, and logs+classifies any unhandled exception before re-raising it). A real bug was found and fixed during this work, not just written correctly the first time: the initial version reset the request-id contextvar in a `finally` block that ran *before* the success-path log line, so every successful request's own completion log showed `request_id: "-"` instead of its real id — caught by an actual live proof (not just unit tests), not assumed correct. **Honest scope boundary**: this covers this app's own `"optimus"` logger only, in a generic method/path/status/duration shape -- it is not yet the specific security-event taxonomy the full Part H item calls for (login failures, rate-limit-exceeded events, billing-webhook failures -- billing doesn't exist yet --, Square API failures, per-shop OpenAI cost -- no shop/tenant model exists yet), and no external log aggregation, dashboards, or alerting were configured (none exist; nothing here should be read as "monitoring is operational"). Verified: 4 new tests in `tests/test_observability.py`, including a real log-exposure test (drives a real login request through `TestClient` with a real-looking password and an OpenAI-key-shaped header value, then asserts neither string appears anywhere in any captured log record, matching this repo's own required "automated log-exposure tests" bar) -- full suite 295 passed, ruff/pyright/`node --check` clean. Live proof against a real Postgres: real synthetic-owner provisioning and a real `/api/auth/login` call both produced correct, correlated structured JSON log lines with matching `X-Request-ID` response headers, and a genuine unhandled-exception path (a real DB connection failure) was also confirmed to log cleanly with no leaked request body content.

**Multi-instance rate limiting, sub-item detail (2026-07-14):** `app/rate_limit.py::SlidingWindowRateLimiter` (in-process, a plain Python dict) was the only implementation, meaning the configured `MAX_ESTIMATES_PER_MINUTE` limit was silently multiplied by however many app instances ran behind a load balancer -- each instance had its own independent counter. New `RedisSlidingWindowRateLimiter`: a Redis sorted-set-per-key sliding window (score = request timestamp), atomic per check via a Redis pipeline (Redis processes one client's pipelined commands as an uninterrupted batch, so concurrent instances can't interleave mid-check). Falls back to the existing in-process limiter, best-effort, only for the duration of a Redis outage -- catches `RedisError` specifically, logs a warning, and degrades rather than either failing the request open (silently disabling all rate limiting) or closed (turning a transient Redis hiccup into an outage of the public chat/estimate-approval endpoints it guards). New `redis>=5.0,<6` dependency (`redis.asyncio`, already an expected part of this stack -- `REDIS_URL` and `/ready`'s Redis check have existed since early phases; this is the first thing to actually *use* it via the Python client). `app/main.py::get_rate_limiter` now constructs the Redis-backed limiter from `settings.redis_url`, same lazy-singleton-with-invalidation pattern as before. Verified: 4 tests in `tests/test_rate_limit.py` (2 unconditional -- in-process limiter, and the Redis-outage-fallback path constructed with a real-but-wrong port so it genuinely exercises `RedisError` handling; 2 gated behind a real-Redis-reachability check rather than mocked, so they're skipped, not falsely green, when no Redis is available -- new `redis` service container added to the `lint-typecheck-test` CI job so these run for real there, not just locally). Beyond the automated tests, a manual live proof (three separate ad-hoc scripts against a real `redis:7-alpine` container, matching this repo's own precedent for infrastructure-dependent behavior) directly proved: (1) two independent limiter instances with separate Redis client objects, simulating two real app instances, correctly share and enforce one combined limit for the same key -- the exact multi-instance-safety property that was previously entirely absent; (2) the sliding window genuinely expires old entries (a request succeeds again after the window elapses, not just after a fixed reset boundary); (3) stopping the Redis container mid-run correctly triggers the in-process fallback rather than crashing or hanging.

**Approval-token revocation, sub-item detail (2026-07-14):** the `EstimateApprovalRequest.status` column already had `'revoked'` in its DB-level CHECK constraint since the original approval-flow migration — the revoke *action* itself (route, store function, audit event, UI) was simply never built. New migration `018_approval_token_revocation`: `estimate_approval_requests` gains `revoked_at`/`revoked_by_user_id`; `estimate_approval_events`'s `event_type` CHECK constraint gains `'revoked'`. New `app/estimate_store.py::revoke_estimate_approval_request()`: owner-scoped, rejects revoking anything not currently `active` (already-used/expired/revoked all correctly rejected with `422`), records a `revoked` audit event (reusing the existing `decline_reason` audit column for the revoke reason — a deliberate reuse of an existing generic field rather than a new column), and — since `send_estimate_for_approval` only accepts `draft`/`ready` estimates — resets the estimate/revision status back to `ready` so the owner can immediately send a fresh link for the same revision without a new (re-researched) revision. New route `POST /api/estimates/{estimate_id}/approval-requests/{approval_request_id}/revoke`. New `active_approval_request_id` field added to the existing approval-history response so the frontend knows which request to target; new "Revoke approval link" button in the estimate result view, shown only when a link is currently active. Verified: 3 new tests in `tests/test_estimate_approval_api.py` (revoke-and-resend, rejects non-active/missing requests, owner-scoped), full suite 289 passed, ruff/pyright/`node --check` clean, migration upgrade/downgrade round-tripped against real Postgres, and a full live proof over real HTTP against a real running server: sent a real link → confirmed it worked → revoked it with a reason → confirmed the same token now returns `404` from the real public approval-view endpoint → confirmed the audit trail shows `[sent, revoked]` with the reason attached → confirmed a fresh link could be sent immediately after.

**Part I — Staging verification.** Before any deploy: full gate suite, Docker builds, clean-DB migration check, the Part C authenticated Playwright run, a log secret scan, customer-facing HTML/PDF verification, and confirmation no Scheduling code was touched. Produce an exact deployment checklist (backup, pull/deploy exact commit, build, migrate, restart, health check, readiness check, authenticated smoke test, rollback condition, rollback command, post-deploy monitoring). As of 2026-07-13, staging is confirmed running a commit after PR #21 but before PR #22/#23 (see `docs/context/CURRENT_STATE.md`'s Staging Prep section) — the immediate, much smaller, separate task of catching the droplet up to current `main` does not require any of Parts A-H and can happen independently once approved.

**Part J — Release process infrastructure. DONE, awaiting merge (2026-07-14).** Owner directive required: semantic versioning, a machine-readable app version, the deployed version shown in the System UI, migration head + source commit recorded, release notes produced from merged work, a release checklist, a release-candidate branch/tag convention documented, all CI gates required before release, rollback criteria documented, database migration compatibility documented, and the app prevented from reporting itself ready against an unsupported database schema — with an explicit instruction not to deploy or create a release tag without separate approval.
- `app/__init__.py::__version__` is the single source of truth (bumped `7.0.1` → `7.1.0`); `pyproject.toml` and every other hardcoded version-string duplicate found across the repo (`app/services/http.py`'s User-Agent header, `integration/optimus_adapter.py`, `app/static/index.html`'s marketing footer, `tests/test_official_ui.py`) now import or are tested against it — `tests/test_release.py::test_pyproject_version_matches_app_version` prevents future drift.
- New `app/migration_compat.py`: dialect-agnostic (works against both the real Postgres deployment target and the SQLite-based fast test suite) schema-compatibility check comparing the app's Alembic head against the connected database's actual `alembic_version` row, classified into `matched`/`behind`/`unmigrated`/`unsupported`/`unreachable`. Only `unsupported`/`unreachable` block readiness; `behind`/`unmigrated` are tolerated by design, since this repo's own deploy runbook (`optimusctl.sh update` before `migrate`) creates a normal window where new app code briefly runs against old-but-compatible schema — recorded as ADR-012 in `docs/context/DECISIONS.md`.
- `/health` and `/ready` now report `version`, `git_commit` (baked into the Docker image at build time via a new `GIT_COMMIT` build arg, `docker-compose.yml`/`Dockerfile`), and `migration_head`; `/ready` additionally reports `database_migration_revision` and `schema_compatibility`, and its overall `status` degrades to `"degraded"` (not a crash) when the schema is unsupported/unreachable.
- System bay UI gained a "Build" tile showing the running migration head and commit (`app/static/index.html`, `app/static/app.js`).
- New `docs/context/RELEASE_CHECKLIST.md` (versioning scheme, required gates, rollback criteria/procedure, and the release-candidate branch/tag convention — `vMAJOR.MINOR.PATCH` tags, `release/vMAJOR.MINOR.x` branches if needed) and `docs/RELEASE_NOTES.md` (first formal release-notes entry, for 7.1.0, summarizing PRs #27-#31 plus this work).
- Verified: 8 new tests (`tests/test_migration_compat.py` ×6, `tests/test_release.py` ×2) plus extensions to `tests/test_api.py`'s `/health`/`/ready` tests including a new degraded-schema test; full suite green; `ruff`/`pyright`/`node --check` clean; live-proven against a real throwaway Postgres + Redis pair (not just unit tests) — `/health` returned the real commit SHA and migration head, `/ready` returned `schema_compatibility: "matched"` after `alembic upgrade head`, and the served `index.html` confirmed to contain the new Build tile with matching DOM ids to what `app.js` populates.
- **No tag created, no deploy performed** — per the explicit owner constraint. Not yet committed/pushed/PR'd as of this entry.

- **Owner-only pilot first** (real jobs, no customer-facing links) → then **controlled customer pilot** (a handful of real approval links, monitored) — after Parts A-J above.

- [x] Part A — CI enforcement. (2026-07-13, merged; branch-protection recommended but not enabled — needs separate owner approval)
- [x] Part B — Synthetic test-account provisioning. (2026-07-13, merged; live-verified against real Postgres over real HTTP)
- [~] Part C — Authenticated Playwright E2E suite. Core repair workflow DONE (2026-07-13, merged); newer-modules + security-behavior coverage still open.
- [x] Part D — Diagnostics/Inspections auditability (archive/void + audit trail). Implemented and live-verified 2026-07-15; not yet committed/pushed/merged.
- [x] Part E — Technician workflow for Diagnostics/Inspections. Implemented and live-verified 2026-07-15; not yet committed/pushed/merged.
- [x] Part F — Parts/Vendors purchase-order + allocation workflow. Both slices (Purchase Orders, Part Allocation) implemented and live-verified 2026-07-15; Purchase Orders merged via PR #36, Part Allocation not yet merged.
- [~] Part G — Reports completion. Slice 1 (Payment Activity + Technician Time) merged via PR #38 (2026-07-16). Slice 2a (Gross Profit/Margin + Inventory Valuation) merged via PR #39 (2026-07-16). Slice 3 (Parts Usage + Vendor Purchasing) merged via PR #40 (2026-07-16). Slice 4 (Work Order Cycle Time + Comebacks) merged via PR #41 (2026-07-16). Slice 5 (Diagnostic Findings + Inspections) merged via PR #42 (2026-07-17). CSV export implemented and live-verified 2026-07-17; not yet committed/pushed/merged. This is the last Part G item -- once merged, Part G is fully complete.
- [~] Part H — Security/production hardening. Approval-token revocation, multi-instance-safe (Redis-backed) rate limiting, and a structured-logging/request-ID foundation DONE (2026-07-14, merged); threat model, the full security-event taxonomy, OpenAI cost logging, retention/export/deletion policy, and monitoring/alerting requirements all still open.
- [ ] Part I — Staging verification + deployment checklist.
- [x] Part J — Release process infrastructure (semver, version/build display, migration-compatibility guard, release checklist, release notes). Implemented and live-verified 2026-07-14; not yet committed/pushed/merged.
- [ ] Owner-only pilot, then controlled customer pilot.

## Agent assignments

| Agent | Role |
|---|---|
| Claude | Slice coordinator + implementer-of-record: `/project-sync`, explore → plan → implement → verify via `optimus-*` subagents, runs gates, runtime proofs, docs, `/end-session`. Owns one branch at a time. |
| Codex | Independent reviewer of Claude's diffs from a separate worktree, or implementer of a slice while Claude reviews — never both writing the same worktree at once. Suggested split: Codex on frontend-heavy work (invoice HTML/PDF templates), Claude on store/migration/API work. |
| Fable (or other review-only model) | High-scrutiny review at slice boundaries: architecture drift, financial-integrity logic, exposure/isolation review, release-readiness audits, cross-phase planning. Reviews; does not implement mid-slice. |

## Stop conditions

1. Any gate (ruff/pyright/pytest/build/migration) red → slice halts; max 3 repair attempts per gate, then stop with a root-cause writeup.
2. Cross-user isolation test failing → hard stop for the whole project, not just the slice.
3. Any secret/token/internal-pricing leak found in logs, payloads, or documents → hard stop + security review before further feature work.
4. Estimate Approval validation weakened to ease downstream work → automatic reject in review.
5. Previous slice not fully verified (including independent review) → next slice may not start.
6. Any live OpenAI spend, push, merge, deploy, or destructive DB action without current-turn approval → not performed.
7. Two agents needing write access to one worktree → stop and re-sequence.

## GitHub workflow

- One slice = one branch: `feat/work-orders` → `feat/invoices` → `feat/payment-tracking` → `harden/local-mvp` → `ops/staging`.
- Push at every green verification milestone, not just at slice end.
- Merge to `main` only with explicit approval, only after independent review passes.
- Saved to GitHub: source, migrations, tests, `docs/context/`, scripts, `.env.example`, Compose/nginx config.
- Never saved: `.env`, keys/tokens, logs, proof-run screenshots, DB volumes, cache, one-off proof scripts, real customer data.
