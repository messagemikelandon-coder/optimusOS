# Agent Instructions

- A UI test does not pass merely because a click event or HTTP request occurred. The expected authenticated workflow, state change, rendered result, and error handling must be verified. HTTP 401, 404, CORS failures, console exceptions, and silent controls are failed tests.
